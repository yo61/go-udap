# go-udap documentation site — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the scattered plain-Markdown docs with a modern, Diataxis-shaped, versioned documentation site published to GitHub Pages, built with Fumadocs.

**Architecture:** A new Next.js + Fumadocs project lives in `docs/site/`. Content authored as Markdown / MDX under `docs/site/content/docs/` in five top-level sections (`tutorials/`, `how-to/`, `reference/`, `concepts/`, `contributing/`) matching Diataxis quadrants + a contributor area. Static export deployed to GitHub Pages via a new `.github/workflows/docs.yml` workflow. Versioning is folder-based — released versions snapshot into `content/docs/v<X.Y.Z>/` triggered by the Release workflow.

**Tech Stack:** Fumadocs (Next.js 15+ docs framework), React 19, TypeScript, MDX, Node 22+, GitHub Pages, GitHub Actions. Authoring stays Markdown-first; MDX is used only for the structural `<HowTo>` component.

**Spec:** `docs/superpowers/specs/2026-05-13-docs-site-design.md` (commit `f184029`).

---

## File structure

### New files

**Fumadocs project skeleton (created via `create-fumadocs-app` in Task 1.1):**

| Path | Responsibility |
|--|--|
| `docs/site/package.json` | Node deps; build/dev scripts |
| `docs/site/next.config.mjs` | Next.js config, including `output: 'export'` for static gh-pages deploy |
| `docs/site/source.config.ts` | Fumadocs source configuration (content path, MDX plugins) |
| `docs/site/tsconfig.json` | TypeScript config |
| `docs/site/tailwind.config.ts` | Tailwind CSS config (if Fumadocs template uses it) |
| `docs/site/app/layout.tsx` | Root layout (theme, fonts) |
| `docs/site/app/(home)/layout.tsx` | Landing-page layout |
| `docs/site/app/(home)/page.tsx` | Landing page (placeholder in M1, custom in M4) |
| `docs/site/app/docs/[[...slug]]/page.tsx` | Catch-all docs page route |
| `docs/site/app/docs/layout.tsx` | Docs layout (sidebar + version selector) |
| `docs/site/components/how-to.tsx` | MDX `<HowTo>` structural component (M3) |
| `docs/site/lib/source.ts` | Fumadocs source object — defines content tree |
| `docs/site/.gitignore` | Node-style ignore (`node_modules/`, `out/`, `.next/`) |

**Content tree (`docs/site/content/docs/`):**

| Path | Diataxis quadrant | Phase |
|--|--|--|
| `index.mdx` | Docs landing (overview / map of the site) | M1 stub, M3 real |
| `tutorials/index.mdx` | Tutorials section landing | M3 |
| `tutorials/configure-your-first-squeezebox.mdx` | Tutorial | M3 |
| `how-to/index.mdx` | How-to section landing | M2 |
| `how-to/install-go-udap.mdx` | How-to (lifted from README) | M2 |
| `how-to/capture-wire-traces.mdx` | How-to (lifted from playbook) | M2 |
| `how-to/configure-for-dhcp.mdx` | How-to (net-new) | M3 |
| `how-to/configure-for-static-ip.mdx` | How-to (net-new) | M3 |
| `how-to/configure-wifi-wpa2.mdx` | How-to (net-new) | M3 |
| `how-to/discover-on-multi-nic-laptop.mdx` | How-to (net-new) | M3 |
| `how-to/recover-stuck-in-init-mode.mdx` | How-to (net-new) | M3 |
| `how-to/onboard-over-wifi-ad-hoc.mdx` | How-to stub (links to Task #34) | M3 |
| `reference/index.mdx` | Reference section landing | M2 |
| `reference/commands/index.mdx` | Commands overview | M2 |
| `reference/commands/discover.mdx` | Reference for `discover` | M2 |
| `reference/commands/info.mdx` | Reference for `info` | M2 |
| `reference/commands/read.mdx` | Reference for `read` | M2 |
| `reference/commands/get.mdx` | Reference for `get` | M2 |
| `reference/commands/set.mdx` | Reference for `set` | M2 |
| `reference/commands/reboot.mdx` | Reference for `reboot` | M2 |
| `reference/commands/getip.mdx` | Reference for `getip` | M2 |
| `reference/commands/interfaces.mdx` | Reference for `interfaces` | M2 |
| `reference/global-flags.mdx` | Reference for global flags (`--timeout`, `--interface`, `--all-interfaces`, `--retries`, `--verbose`) | M2 |
| `reference/config-file-format.mdx` | Reference (lifted from README) | M2 |
| `reference/nvram-parameters.mdx` | Reference (lifted from README) | M2 |
| `reference/exit-codes.mdx` | Reference (extracted) | M2 |
| `reference/api.mdx` | One-page index pointing at pkg.go.dev | M2 |
| `concepts/index.mdx` | Explanation section landing | M2 |
| `concepts/what-is-go-udap.mdx` | Explanation (lifted from README Overview) | M2 |
| `concepts/squeezeplay-comparison.mdx` | Explanation (lifted from docs/notes/) | M2 |
| `concepts/how-udap-discovery-works.mdx` | Explanation (net-new) | M3 |
| `concepts/multi-nic-discovery.mdx` | Explanation (net-new) | M3 |
| `concepts/the-26-nvram-parameters.mdx` | Explanation (net-new) | M3 |
| `contributing/index.mdx` | Contributor area landing | M2 |
| `contributing/building-from-source.mdx` | Contributor docs (lifted from DEVELOPMENT.md) | M2 |
| `contributing/cross-compilation.mdx` | Contributor docs (lifted from DEVELOPMENT.md) | M2 |
| `contributing/release-process.mdx` | Contributor docs (extracted) | M2 |

**Downloadable example config files (`docs/site/content/examples/`):**

| Path | Content |
|--|--|
| `dhcp.conf` | `interface=1`, `lan_ip_mode=1` plus comments | M3 |
| `static-ip.conf` | Static IP example | M3 |
| `wifi-wpa2.conf` | WPA2 wireless example | M3 |

**CI workflow:**

| Path | Responsibility |
|--|--|
| `.github/workflows/docs.yml` | Build + deploy on push to `main` when `docs/site/**` changes | M1 |

### Modified files

| Path | Change |
|--|--|
| `README.md` | Reduced to thin landing page (~30-50 lines) pointing at the published docs site | M2 |
| `.github/workflows/release.yml` (existing semantic-release workflow) | Add a post-release step that snapshots `docs/site/content/docs/` → `docs/site/content/docs/v<X.Y.Z>/` and pushes it | M4 |

### Deleted files

| Path | When |
|--|--|
| `DEVELOPMENT.md` | M2 (content moved to `contributing/`) |
| `docs/notes/squeezeplay-comparison.md` | M2 (content moved to `concepts/`) |
| `docs/playbooks/sbr-capture.md` | M2 (content moved to `how-to/`) |
| `docs/notes/` (directory, if empty after the above) | M2 |
| `docs/playbooks/` (directory, if empty after the above) | M2 |

---

## Milestone M1 — Skeleton deploys

**Branch:** `feat/docs-site-m1-skeleton`. Branched from `main`.
**PR title:** `feat(docs): scaffold Fumadocs site with GitHub Pages deploy`

### Task 1.1: Scaffold the Fumadocs project

**Files:** Many new files under `docs/site/` (generated by the Fumadocs template).

- [ ] **Step 1: Confirm Node 22+ is available**

```bash
node --version
```

Expected: `v22.x.x` or later. If not, install Node 22 via `nvm` or the user's preferred manager.

- [ ] **Step 2: Confirm pnpm is available (preferred for Fumadocs)**

```bash
pnpm --version
```

If not installed: `npm install -g pnpm` or `corepack enable pnpm`.

- [ ] **Step 3: Scaffold the Fumadocs project**

From the repo root:

```bash
cd docs
pnpm create fumadocs-app site
```

When prompted, choose:
- Template: **Next.js / Default (Fumadocs UI)**
- Use MDX: **yes**
- Package manager: **pnpm**
- TypeScript: **yes**
- Tailwind CSS: **yes** (default)

This creates `docs/site/` with a full Fumadocs scaffold (~30 files).

- [ ] **Step 4: Verify dev server runs locally**

```bash
cd docs/site
pnpm install
pnpm dev
```

Expected: dev server starts on `http://localhost:3000`, the default Fumadocs landing page renders.

Stop the dev server (`Ctrl-C`).

- [ ] **Step 5: Verify static export works**

```bash
pnpm build
```

Expected: build completes; `docs/site/out/` directory created with static HTML files.

- [ ] **Step 6: Commit the scaffolded site**

```bash
cd /Users/robin/code/github/yo61/go-udap   # back to repo root
git add docs/site
git commit -m "feat(docs): scaffold Fumadocs project skeleton"
```

### Task 1.2: Configure Next.js for GitHub Pages deployment

**Files:**
- Modify: `docs/site/next.config.mjs`

GitHub Pages serves the site under a subpath: `https://yo61.github.io/go-udap/`. Next.js needs `basePath` set to `/go-udap` for asset URLs to resolve. Static export must be enabled.

- [ ] **Step 1: Inspect current `next.config.mjs`**

```bash
cat docs/site/next.config.mjs
```

It will look like (exact wording may differ slightly with Fumadocs versions):

```javascript
import { createMDX } from 'fumadocs-mdx/next';

const withMDX = createMDX();

/** @type {import('next').NextConfig} */
const config = {
  reactStrictMode: true,
};

export default withMDX(config);
```

- [ ] **Step 2: Edit to add static export + basePath**

Replace the contents with:

```javascript
import { createMDX } from 'fumadocs-mdx/next';

const withMDX = createMDX();

/** @type {import('next').NextConfig} */
const config = {
  reactStrictMode: true,
  output: 'export',
  basePath: process.env.BASE_PATH ?? '/go-udap',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

export default withMDX(config);
```

The `BASE_PATH` env var override lets the local dev server use no base path (`pnpm dev` runs without `BASE_PATH` set, so `basePath` is `/go-udap` in build but blank locally if you `export BASE_PATH=""` first).

- [ ] **Step 3: Verify the build still works under the new config**

```bash
cd docs/site
pnpm build
```

Expected: build completes; `out/` directory populated; spot-check `out/index.html` references assets via `/go-udap/...` paths.

- [ ] **Step 4: Verify local dev still works**

```bash
BASE_PATH= pnpm dev
```

Expected: dev server starts; `http://localhost:3000` renders the default page (no `/go-udap` prefix locally).

- [ ] **Step 5: Commit**

```bash
git add docs/site/next.config.mjs
git commit -m "feat(docs): configure Next.js for GitHub Pages static export"
```

### Task 1.3: Replace the placeholder home page

**Files:**
- Modify: `docs/site/app/(home)/page.tsx` (or wherever the home page lives in the Fumadocs template)

- [ ] **Step 1: Inspect the current home page**

```bash
find docs/site/app -name 'page.tsx' | head
cat docs/site/app/\(home\)/page.tsx 2>/dev/null || cat docs/site/app/page.tsx
```

- [ ] **Step 2: Replace contents with a thin placeholder**

Write to `docs/site/app/(home)/page.tsx` (use whichever path the template used):

```typescript
export default function HomePage() {
  return (
    <main className="flex flex-1 flex-col justify-center text-center px-4 py-16">
      <h1 className="mb-4 text-4xl font-bold">go-udap</h1>
      <p className="text-fd-muted-foreground mb-8">
        Squeezebox UDAP configuration tool — documentation site under construction.
      </p>
      <p className="text-fd-muted-foreground">
        <a className="text-fd-foreground underline" href="/go-udap/docs">
          Browse the docs
        </a>
      </p>
    </main>
  );
}
```

- [ ] **Step 3: Verify the page renders**

```bash
cd docs/site
pnpm dev
```

Visit `http://localhost:3000`; expected to see the placeholder. Stop the dev server.

- [ ] **Step 4: Commit**

```bash
git add docs/site/app
git commit -m "feat(docs): replace template landing page with placeholder"
```

### Task 1.4: Add a single placeholder docs page

**Files:**
- Create: `docs/site/content/docs/index.mdx`

The Fumadocs scaffold may already have demo content; replace it with one minimal page so the site has just the bare entry.

- [ ] **Step 1: Remove demo content from `docs/site/content/docs/`**

```bash
ls docs/site/content/docs/
```

Delete any files / subdirectories that came from the template (typical Fumadocs demo includes a `test/` or sample MDX files).

```bash
rm -rf docs/site/content/docs/*
```

- [ ] **Step 2: Create the docs landing page**

Write to `docs/site/content/docs/index.mdx`:

```mdx
---
title: go-udap docs
description: Squeezebox UDAP configuration tool documentation
---

# go-udap documentation

This site is under construction. The first usable content lands in M2;
net-new tutorials and how-to guides land in M3.

For now, the source repo is at
[github.com/yo61/go-udap](https://github.com/yo61/go-udap) and the
README contains the canonical user-facing docs.
```

- [ ] **Step 3: Verify it renders**

```bash
pnpm dev
```

Visit `http://localhost:3000/docs`. Expected: the page above renders.

- [ ] **Step 4: Commit**

```bash
git add docs/site/content/docs
git commit -m "feat(docs): seed docs landing page placeholder"
```

### Task 1.5: Create the GitHub Actions deploy workflow

**Files:**
- Create: `.github/workflows/docs.yml`

- [ ] **Step 1: Write the workflow file**

Create `.github/workflows/docs.yml`:

```yaml
name: Docs

on:
  push:
    branches: [main]
    paths:
      - 'docs/site/**'
      - '.github/workflows/docs.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: docs/site
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: docs/site/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: docs/site/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 2: Commit the workflow**

```bash
git add .github/workflows/docs.yml
git commit -m "ci(docs): add GitHub Pages deploy workflow"
```

### Task 1.6: Enable GitHub Pages in repo settings (manual user step)

This is a one-time GitHub web-UI action; the agent should NOT attempt to perform it.

- [ ] **Step 1: Tell the user to enable Pages**

Open `https://github.com/yo61/go-udap/settings/pages`. Set:

- **Source:** "GitHub Actions"

(Do NOT pick "Deploy from a branch"; the workflow created in Task 1.5 uses the new Actions-based deploy.)

Save the setting. No additional config needed — the workflow handles everything.

- [ ] **Step 2: Confirm by pushing the M1 branch and watching the Actions tab**

After this entire M1 milestone has been committed and pushed, the workflow will run; check `https://github.com/yo61/go-udap/actions` for a green "Docs" workflow run.

### Task 1.7: Verify dev server end-to-end one final time

- [ ] **Step 1: Clean local dev**

```bash
cd docs/site
rm -rf .next out node_modules
pnpm install --frozen-lockfile
pnpm dev
```

Expected: clean install, dev server starts, all placeholder pages render.

- [ ] **Step 2: Clean static build**

```bash
pnpm build
```

Expected: clean build, `out/` populated, no errors.

### Task 1.8: Push and open the M1 PR

- [ ] **Step 1: Push the branch**

```bash
git push -u origin feat/docs-site-m1-skeleton
```

- [ ] **Step 2: Open the PR**

```bash
gh pr create --title "feat(docs): scaffold Fumadocs site with GitHub Pages deploy" --body "Closes M1 of Task #28. Lands an empty-but-deployable Fumadocs site with the GitHub Actions deploy workflow.

## What's included
- Fumadocs project scaffold under docs/site/
- Next.js configured for static export with basePath=/go-udap
- Placeholder home page and docs landing page
- GitHub Actions workflow that builds + deploys on push to main when docs/site/ changes

## Test plan
- [x] pnpm dev serves locally
- [x] pnpm build produces static export
- [ ] After merge: GitHub Pages serves the site at https://yo61.github.io/go-udap/
- [ ] After merge: docs.yml workflow goes green"
```

- [ ] **Step 3: After merge, verify the deploy**

Visit `https://yo61.github.io/go-udap/`. Expected: the placeholder home page renders. Visit `/go-udap/docs/` — the docs landing renders.

---

## Milestone M2 — Existing content classified and live

**Branch:** `feat/docs-site-m2-content`. Branched from `main` after M1 has merged.
**PR title:** `feat(docs): port existing docs into Diataxis quadrants`

This milestone has many small tasks — one per existing-source classification, plus cleanup of the original files. Tasks within this milestone CAN be committed individually for cleaner history, but they all land in one PR.

### Task 2.1: Set up the docs content section landings

**Files:**
- Create: `docs/site/content/docs/tutorials/index.mdx` (stub for now; M3 populates the tutorial)
- Create: `docs/site/content/docs/how-to/index.mdx`
- Create: `docs/site/content/docs/reference/index.mdx`
- Create: `docs/site/content/docs/concepts/index.mdx`
- Create: `docs/site/content/docs/contributing/index.mdx`

- [ ] **Step 1: Write each section landing**

`docs/site/content/docs/tutorials/index.mdx`:

```mdx
---
title: Tutorials
description: Learn go-udap by doing
---

# Tutorials

Hands-on, learning-oriented walkthroughs for someone new to go-udap.
Pick one and follow it end-to-end — every step has expected output so
you can confirm you're on track.

(More tutorials coming as use cases emerge.)
```

`docs/site/content/docs/how-to/index.mdx`:

```mdx
---
title: How-to guides
description: Task-oriented recipes
---

# How-to guides

Each page solves one specific problem. Find the goal that matches
yours, check the prerequisites, follow the steps, verify the result.
```

`docs/site/content/docs/reference/index.mdx`:

```mdx
---
title: Reference
description: Technical descriptions of go-udap's surface
---

# Reference

Authoritative lookup material — every subcommand, every flag, every
NVRAM parameter, every exit code. Skim or grep.
```

`docs/site/content/docs/concepts/index.mdx`:

```mdx
---
title: Concepts
description: How go-udap works, and why
---

# Concepts

Explanation-oriented pages that fill in the *why*: protocol shapes,
design decisions, the things you'd want to understand if your
discovery isn't returning what you expect.
```

`docs/site/content/docs/contributing/index.mdx`:

```mdx
---
title: Contributing
description: Building, testing, and developing go-udap
---

# Contributing

If you're working ON go-udap (rather than WITH it), start here.
```

- [ ] **Step 2: Verify renders**

```bash
cd docs/site
pnpm dev
```

Visit `http://localhost:3000/go-udap/docs/tutorials`, `.../how-to`, etc. — all five section landings render. Stop the dev server.

- [ ] **Step 3: Commit**

```bash
git add docs/site/content/docs
git commit -m "docs(site): seed five Diataxis section landings"
```

### Task 2.2: Port README §Overview → `concepts/what-is-go-udap.mdx`

**Files:**
- Create: `docs/site/content/docs/concepts/what-is-go-udap.mdx`

- [ ] **Step 1: Read the source content**

Read `README.md` lines covering the "Overview" section (typically lines 1–20).

- [ ] **Step 2: Write the new page**

```mdx
---
title: What is go-udap?
description: The tool, the protocol, and what they do
---

# What is go-udap?

go-udap is a command-line tool for discovering and configuring
Squeezebox devices over the [Universal Device Access Protocol
(UDAP)](https://wiki.lyrion.org/index.php/UDAP) on UDP port 17784.

It lets you:

- Discover Squeezebox devices on your local network
- Read and write NVRAM parameters (network config, wireless, server
  address, hostname, and 20+ others)
- Trigger a device reboot to apply configuration changes
- Query a device's network configuration directly

The tool is single-shot CLI: every operation is one invocation. There
is no daemon, no persistent state, no interactive shell. Subcommands
correspond to UDAP wire methods (`discover`, `get_ip`, `get_data`,
`set_data`, `reset`).

## Why does go-udap exist?

The reference Squeezebox configuration tool, Logitech's
`SetupSqueezeboxApplet`, was a desktop app whose dependencies have
bit-rotted. The Net::UDAP Perl reference implementation still works
but pulls a heavy Perl runtime. go-udap is a single static binary
(~2.8 MB) you can drop on macOS, Linux, or Windows.
```

- [ ] **Step 3: Verify it renders**

Run `pnpm dev`; visit `.../concepts/what-is-go-udap`. Spot-check.

- [ ] **Step 4: Commit**

```bash
git add docs/site/content/docs/concepts/what-is-go-udap.mdx
git commit -m "docs(site): port README Overview → concepts/what-is-go-udap"
```

### Task 2.3: Port README §Installation → `how-to/install-go-udap.mdx`

**Files:**
- Create: `docs/site/content/docs/how-to/install-go-udap.mdx`

- [ ] **Step 1: Lift the README installation content**

Read README §Installation (Homebrew tap, pre-built binaries, build from source).

- [ ] **Step 2: Write the new page**

```mdx
---
title: Install go-udap
description: Get the go-udap binary onto your machine
---

# Install go-udap

Three options, in order of preference for most users.

## Option 1 — Homebrew (macOS and Linux)

```bash
brew install yo61/tap/go-udap
```

Updates flow through `brew upgrade` like any other formula. This is the
fastest path for users who already use Homebrew.

## Option 2 — Pre-built binaries

Download the archive for your platform from the
[Releases](https://github.com/yo61/go-udap/releases) page:

- `go-udap_<version>_macos_arm64.tar.gz` (Apple Silicon)
- `go-udap_<version>_macos_amd64.tar.gz` (Intel Mac)
- `go-udap_<version>_linux_amd64.tar.gz`
- `go-udap_<version>_linux_arm64.tar.gz`
- `go-udap_<version>_windows_amd64.zip`

Each release also has a `SHA256SUMS` file you can verify against:

```bash
shasum -a 256 -c SHA256SUMS
```

Extract the archive; put the `go-udap` (or `go-udap.exe`) binary on
your `PATH`.

## Option 3 — Build from source

Requires Go 1.26 or later.

```bash
git clone https://github.com/yo61/go-udap.git
cd go-udap
go build -o go-udap .
```

The repo also has a [Taskfile](https://taskfile.dev) for cross-compile
and release tasks; see the [Contributing](/docs/contributing) section
if you're working on the tool itself.

## Verify the install

```bash
go-udap --version
```

Expected: prints the installed version.
```

- [ ] **Step 3: Verify renders and links**

Run `pnpm dev`; visit `.../how-to/install-go-udap`; check the link to `/docs/contributing` resolves.

- [ ] **Step 4: Commit**

```bash
git add docs/site/content/docs/how-to/install-go-udap.mdx
git commit -m "docs(site): port README Installation → how-to/install-go-udap"
```

### Task 2.4: Port README §Usage / Commands table → `reference/commands/`

**Files:**
- Create: `docs/site/content/docs/reference/commands/index.mdx`
- Create: `docs/site/content/docs/reference/commands/discover.mdx`
- Create: `docs/site/content/docs/reference/commands/info.mdx`
- Create: `docs/site/content/docs/reference/commands/read.mdx`
- Create: `docs/site/content/docs/reference/commands/get.mdx`
- Create: `docs/site/content/docs/reference/commands/set.mdx`
- Create: `docs/site/content/docs/reference/commands/reboot.mdx`
- Create: `docs/site/content/docs/reference/commands/getip.mdx`
- Create: `docs/site/content/docs/reference/commands/interfaces.mdx`

- [ ] **Step 1: Write the commands index**

`docs/site/content/docs/reference/commands/index.mdx`:

```mdx
---
title: Commands
description: Every go-udap subcommand
---

# Commands

| Command | Purpose |
|---|---|
| [`discover`](./discover) | Discover devices on the network |
| [`info`](./info) | Show metadata for one device |
| [`read`](./read) | Read NVRAM parameters from a device |
| [`get`](./get) | Read specific parameters by name |
| [`set`](./set) | Write NVRAM parameters |
| [`reboot`](./reboot) | Reboot the device |
| [`getip`](./getip) | Query the device's IP / subnet / gateway |
| [`interfaces`](./interfaces) | List host network interfaces usable for discovery |

Global flags that apply to every command are documented under
[Global flags](../global-flags).
```

- [ ] **Step 2: Write `discover.mdx`**

```mdx
---
title: discover
description: Discover Squeezebox devices on the network
---

# `discover`

```text
go-udap discover [--info] [--timeout DURATION]
```

## What it does

Broadcasts a UDAP advanced-discovery request (UCP method `0x0009`) to
`255.255.255.255:17784` and prints the MAC address of every device
that replies within the timeout window.

With `--info`, prints a multi-line metadata block per device
(MAC / IP / Name / Model / Firmware / HW Rev / UUID / State) and
fires a per-device `get_ip` to show the device's current network
config (IP / Subnet / Gateway). Per-device `get_ip` failures are
soft — the table shows dashes, and a `--verbose` warning is logged
to stderr.

## Output

- Without `--info`: one MAC per line, sorted by MAC string.
- With `--info`: per-device metadata block, blank line between
  devices.

## Flags

| Flag | Default | Description |
|---|---|---|
| `--info` | off | Print full metadata per device including `get_ip` results |
| `--timeout DURATION` | `5s` | How long to wait for device replies |

## Exit codes

- `0` — success (even if zero devices found)
- `2` — discovery failed (transport error)

## Examples

Quick scan:

```bash
go-udap discover
# 00:04:20:16:06:02
```

Full metadata:

```bash
go-udap discover --info
```

On a multi-NIC host, scope discovery to one interface:

```bash
go-udap --interface en7 discover
```

See [How to discover on a multi-NIC laptop](/docs/how-to/discover-on-multi-nic-laptop)
for the longer story.
```

- [ ] **Step 3: Write `info.mdx`**

```mdx
---
title: info
description: Show metadata for one device
---

# `info`

```text
go-udap info <mac> [--timeout DURATION]
```

## What it does

Discovers devices on the network, then prints the full metadata block
for the one whose MAC matches `<mac>`. Same metadata as
`discover --info` but scoped to one device.

Also fires `get_ip` to populate IP / Subnet / Gateway, and (if the
device's discovery TLV 0x0d was missing) falls back to `get_uuid`
to populate UUID.

## Output

Multi-line metadata block, one field per line.

## Flags

| Flag | Default | Description |
|---|---|---|
| `--timeout DURATION` | `5s` | Operation timeout |

## Exit codes

- `0` — success
- `1` — invalid MAC argument
- `2` — device not found, or transport error

## Example

```bash
go-udap info 00:04:20:16:06:02
```
```

- [ ] **Step 4: Write `read.mdx`**

```mdx
---
title: read
description: Read NVRAM parameters from a device
---

# `read`

```text
go-udap read <mac> [--all|-a] [--timeout DURATION]
```

## What it does

Reads every recognised NVRAM parameter (the 26 in
[`udap.Parameters`](../nvram-parameters)) from the device and prints
them as `param=value` lines, sorted by parameter name.

By default, factory-default values are filtered out so the output
round-trips cleanly through [`set --config FILE`](./set). Pass
`--all` (or `-a`) to include factory defaults *and* unrecognised
`offset_NNN` entries.

## Output

`param=value` lines, one per parameter, sorted by name. Suitable for
redirection to a `.conf` file.

## Flags

| Flag | Default | Description |
|---|---|---|
| `--all`, `-a` | off | Include factory-default values and unknown-offset entries |
| `--timeout DURATION` | `5s` | Operation timeout |

## Exit codes

- `0` — success
- `1` — invalid MAC argument
- `2` — device not found, or transport error

## Examples

```bash
# Back up a device's non-default settings
go-udap read 00:04:20:16:06:02 > backup.conf

# Restore later
go-udap set 00:04:20:16:06:02 --config backup.conf --reboot
```
```

- [ ] **Step 5: Write `get.mdx`**

```mdx
---
title: get
description: Read specific parameters from a device
---

# `get`

```text
go-udap get <mac> <param> [<param> ...] [--timeout DURATION]
```

## What it does

Reads one or more named NVRAM parameters from the device.

- Single parameter: prints the bare value (no key=).
- Multiple parameters: prints `param=value` lines in request order.

Useful for scripting where you want a single value:

```bash
ip=$(go-udap get 00:04:20:16:06:02 lan_network_address)
```

## Output

- One value, no label, on single-param queries.
- `param=value` lines on multi-param queries.

## Flags

| Flag | Default | Description |
|---|---|---|
| `--timeout DURATION` | `5s` | Operation timeout |

## Exit codes

- `0` — success
- `1` — invalid MAC argument or unknown parameter name
- `2` — device not found, or transport error
```

- [ ] **Step 6: Write `set.mdx`**

```mdx
---
title: set
description: Write NVRAM parameters to a device
---

# `set`

```text
go-udap set <mac> [--reboot|-r] [--config FILE] [--<param> VALUE ...] [--timeout DURATION]
```

## What it does

Writes one or more NVRAM parameters to the device. The wire op writes
NVRAM directly — every `UCP_METHOD_SET_DATA` writes; there is no
separate `save_data` method.

Parameter values are layered from three sources, in the following
priority (later wins):

1. `--config FILE` — INI-format `key=value` file
2. Piped stdin (use `--config -` or just pipe to stdin)
3. Per-parameter flags (`--hostname newname`, `--lan-ip-mode 1`, etc.)

Pass `--reboot`/`-r` to also reboot the device afterwards. Some
changes (e.g. network config) only take effect after reboot.

## Flags

| Flag | Default | Description |
|---|---|---|
| `--config FILE` | — | INI-format config file. Use `-` to read from stdin |
| `--reboot`, `-r` | off | Also reboot the device after writing |
| `--timeout DURATION` | `5s` | Operation timeout |
| `--<param> VALUE` | — | One flag per parameter you want to set (see [NVRAM parameters](../nvram-parameters) for the canonical list) |

## Examples

DHCP on wireless with WPA2:

```bash
go-udap set 00:04:20:16:06:02 \
  --interface 0 --lan-ip-mode 1 \
  --wireless-ssid SlimNet --wireless-wpa-on 1 --wireless-wpa-mode 2 \
  --wireless-wpa-psk 'shared-secret' \
  --server-address 192.168.1.250 \
  --reboot
```

Restore from a backup:

```bash
go-udap set 00:04:20:16:06:02 --config backup.conf --reboot
```

Pipe parameters from stdin:

```bash
go-udap set 00:04:20:16:06:02 <<EOF
interface=1
lan_ip_mode=0
lan_network_address=192.168.1.50
lan_subnet_mask=255.255.255.0
lan_gateway=192.168.1.1
EOF
```

## Exit codes

- `0` — success
- `1` — invalid MAC, unknown parameter, or malformed config
- `2` — device not found, or transport error

See [Config file format](../config-file-format) for the `.conf` syntax.
```

- [ ] **Step 7: Write `reboot.mdx`**

```mdx
---
title: reboot
description: Reboot the device
---

# `reboot`

```text
go-udap reboot <mac> [--timeout DURATION]
```

## What it does

Sends `UCP_METHOD_RESET` (`0x0004`) to the device. The device reboots,
which takes ~10 seconds during which it ignores all UDP traffic.

`go-udap reboot` returns success once the request has been sent; it
does NOT wait for the device to finish rebooting.

## Flags

| Flag | Default | Description |
|---|---|---|
| `--timeout DURATION` | `5s` | Operation timeout |

## Exit codes

- `0` — success
- `1` — invalid MAC
- `2` — device not found, or transport error
```

- [ ] **Step 8: Write `getip.mdx`**

```mdx
---
title: getip
description: Query the device's IP / subnet / gateway
---

# `getip`

```text
go-udap getip <mac> [--timeout DURATION]
```

## What it does

Sends `UCP_METHOD_GET_IP` (`0x0002`) to the device and prints the
device's current IP, subnet mask, and gateway as three labelled lines.

Distinct from discovery: discovery passively observes (the source IP
of the device's discovery reply); `getip` actively asks the device
for its own view of its network state. Useful after a `set` to verify
the new config took effect.

## Output

```text
IP:      192.168.1.50
Subnet:  255.255.255.0
Gateway: 192.168.1.1
```

If the device doesn't have a network config yet (factory state),
each line shows `-`.

## Flags

| Flag | Default | Description |
|---|---|---|
| `--timeout DURATION` | `5s` | Operation timeout |

## Exit codes

- `0` — success
- `1` — invalid MAC
- `2` — device not found, or transport error
```

- [ ] **Step 9: Write `interfaces.mdx`**

```mdx
---
title: interfaces
description: List network interfaces usable for discovery
---

# `interfaces`

```text
go-udap interfaces
```

## What it does

Lists every local network interface that's usable for UDAP discovery.
Filter applied: `Up + Broadcast + !Loopback + has IPv4 address`. The
output gives you the `NAME` values you can pass to `--interface`.

## Output

Fixed-column table:

```text
NAME            INDEX  ADDRESS            BROADCAST
en0             14     192.168.1.118      192.168.1.255
en7             31     192.168.1.226      192.168.1.255
```

The `BROADCAST` column is the subnet's directed-broadcast address.
It's informational only — UDAP discovery sends to limited broadcast
`255.255.255.255` regardless (see
[How UDAP discovery works](/docs/concepts/how-udap-discovery-works)
for the why).

## Exit codes

- `0` — success (even if zero interfaces qualify)
- `2` — OS enumeration error
```

- [ ] **Step 10: Verify all command pages render**

Run `pnpm dev`; visit each `/docs/reference/commands/<name>` URL.

- [ ] **Step 11: Commit**

```bash
git add docs/site/content/docs/reference/commands
git commit -m "docs(site): port command reference pages (one per subcommand)"
```

### Task 2.5: Port global flags → `reference/global-flags.mdx`

**Files:**
- Create: `docs/site/content/docs/reference/global-flags.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Global flags
description: Flags that apply to every go-udap subcommand
---

# Global flags

Every flag below can appear before OR after the subcommand —
`go-udap -v read <mac>` and `go-udap read -v <mac>` are equivalent.

| Flag | Default | Purpose |
|---|---|---|
| `--timeout DURATION` | `5s` | Operation timeout, e.g. `5s`, `30s`, `2m` |
| `--verbose`, `-v` | off | Debug logging to stderr. Also surfaces soft-fail warnings (e.g. `get_ip` timeout in `discover --info`) that are silent by default |
| `--interface NAME` | — | Bind discovery (and all subsequent operations) to one named network interface. See [`interfaces`](./commands/interfaces) for valid names. Mutually exclusive with `--all-interfaces`. **Not supported on Windows** — the flag is hidden from `--help` there |
| `--all-interfaces` | off | Fan out discovery across every usable network interface. Useful on multi-homed hosts. Mutually exclusive with `--interface`. **Not supported on Windows** |
| `--retries N` | `0` | Re-transmit each UDAP send `N` additional times back-to-back. Useful on lossy links (ad-hoc Wi-Fi). `--retries 2` matches the squeezeplay reference triple-send |
| `--version` | — | Print version and exit |
| `--help`, `-h` | — | Print help |

## Multi-interface behaviour (`--interface` / `--all-interfaces`)

Discovery only emits broadcasts on the kernel's default-route NIC by
default. On a multi-homed laptop (Wi-Fi + Ethernet both up), if the
Squeezebox is on the non-default subnet you'll see zero devices.

`--interface NAME` forces UDAP traffic out a specific NIC via
`IP_BOUND_IF` (macOS) / `SO_BINDTODEVICE` (Linux). `--all-interfaces`
sends concurrently on every NIC.

See [How to discover on a multi-NIC laptop](/docs/how-to/discover-on-multi-nic-laptop)
for usage, and [Multi-NIC discovery](/docs/concepts/multi-nic-discovery)
for the underlying mechanics.
```

- [ ] **Step 2: Verify it renders**

Run `pnpm dev`; visit `/docs/reference/global-flags`.

- [ ] **Step 3: Commit**

```bash
git add docs/site/content/docs/reference/global-flags.mdx
git commit -m "docs(site): port global flags reference"
```

### Task 2.6: Port README §Config file format → `reference/config-file-format.mdx`

**Files:**
- Create: `docs/site/content/docs/reference/config-file-format.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Config file format
description: INI syntax for go-udap set --config FILE
---

# Config file format

`go-udap set --config FILE` reads INI-style configuration:

- One `key=value` per line
- Whitespace around `=` is allowed and stripped
- Lines starting with `#` or `;` are comments
- Blank lines are ignored
- Keys must be canonical UDAP parameter names (e.g. `wireless_SSID`,
  not `wireless-ssid`)

The format matches the output of [`go-udap read`](./commands/read), so
round-tripping through `read | tee backup.conf` then
`set --config backup.conf` works without conversion.

## Example

```ini
# Network
interface=1
lan_ip_mode=1

# Wireless
wireless_SSID=MyNet
wireless_wpa_on=1
wireless_wpa_psk=secret

# Server
server_address=192.168.1.250
```

See [NVRAM parameters](./nvram-parameters) for the full list of valid
keys, their types, and their ranges.
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev   # spot-check
git add docs/site/content/docs/reference/config-file-format.mdx
git commit -m "docs(site): port config file format reference"
```

### Task 2.7: Port README §Configuration Parameters → `reference/nvram-parameters.mdx`

**Files:**
- Create: `docs/site/content/docs/reference/nvram-parameters.mdx`

- [ ] **Step 1: Lift the README parameter tables**

Read README's Configuration Parameters section (Network / Server /
Wireless / Wireless security WEP / Wireless security WPA-WPA2 /
Factory reset). Five tables plus prose.

- [ ] **Step 2: Write the new page**

(Content is lifted verbatim from README. To save plan length, the
implementer copies each table as-is. See README.md
`## Configuration Parameters` section for the source.)

Template structure:

```mdx
---
title: NVRAM parameters
description: The 26 known UDAP NVRAM parameters, by category
---

# NVRAM parameters

go-udap knows 26 canonical parameters. These are the only ones
[`set`](./commands/set) accepts as `--<param>` flags, and the only
ones [`read`](./commands/read) reports. Unknown parameters returned
by the device appear as `offset_NNN` entries (visible with
`read --all`).

## Network

| Parameter | Type | Description |
|-----------|------|-------------|
| `lan_ip_mode` | Integer (0-1) | 0 = Static IP, 1 = DHCP |
| `lan_network_address` | IPv4 | Static IP address |
| ... | ... | ... |

(Copy from README.md verbatim. Five sections: Network, Server,
Wireless, Wireless security — WEP, Wireless security — WPA/WPA2.
Plus the "Factory reset" prose note.)
```

The implementer should literally copy each table block from README
into the new page. The aliases (`squeezecenter_address`,
`slimserver_address`, `lms_address`) all point at the same offset;
the page should note that aliases are accepted on input but `read`
emits the canonical name.

- [ ] **Step 3: Verify the tables render correctly in the Fumadocs theme**

Run `pnpm dev`; visit `/docs/reference/nvram-parameters`. Wide tables
may need responsive CSS; Fumadocs's default theme handles this OK on
most viewports.

- [ ] **Step 4: Commit**

```bash
git add docs/site/content/docs/reference/nvram-parameters.mdx
git commit -m "docs(site): port NVRAM parameters reference"
```

### Task 2.8: Add exit codes reference → `reference/exit-codes.mdx`

**Files:**
- Create: `docs/site/content/docs/reference/exit-codes.mdx`

go-udap uses three exit codes consistently. Pull them into one
referenceable page.

- [ ] **Step 1: Write the page**

```mdx
---
title: Exit codes
description: What go-udap's exit codes mean
---

# Exit codes

| Code | Meaning |
|---|---|
| `0` | Success |
| `1` | Usage error — bad MAC, unknown parameter name, conflicting flags, missing required argument, unknown interface name |
| `2` | Operation failure — device not found, transport error, timeout, MethodError reply |

Each subcommand's reference page lists the specific conditions that
produce code `1` vs `2` for that command.
```

- [ ] **Step 2: Commit**

```bash
git add docs/site/content/docs/reference/exit-codes.mdx
git commit -m "docs(site): add exit codes reference"
```

### Task 2.9: Add API reference index → `reference/api.mdx`

**Files:**
- Create: `docs/site/content/docs/reference/api.mdx`

The API reference itself is hosted on pkg.go.dev (free, auto-updated).
This page is a one-page index pointing readers there with brief
guidance on what packages exist.

- [ ] **Step 1: Write the page**

```mdx
---
title: Library API
description: Using go-udap as a Go library
---

# Library API

go-udap is primarily a CLI but the underlying packages are usable as a
Go library. The authoritative API reference is auto-generated and
hosted on **pkg.go.dev**:

[**→ pkg.go.dev/go-udap**](https://pkg.go.dev/go-udap)

## Packages

| Package | Purpose |
|---|---|
| [`udap`](https://pkg.go.dev/go-udap/udap) | Core client, packet types, parameter table, transports. The main public API |
| [`mocksbr`](https://pkg.go.dev/go-udap/mocksbr) | In-process Squeezebox mock for testing. Implements the same wire protocol as a real device |

## Quick example

```go
package main

import (
	"context"
	"fmt"
	"time"

	"go-udap/udap"
)

func main() {
	client, _ := udap.NewClient()
	defer client.Close()

	ctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)
	defer cancel()
	_ = client.DiscoverDevicesWithContext(ctx)

	for _, d := range client.ListDevices() {
		fmt.Printf("%s  %s\n", d.MAC, d.Name)
	}
}
```

All operations take a `context.Context`. There are no
timeout-parameter-based legacy shims; cancellation and deadlines are
the only way to bound an operation.
```

- [ ] **Step 2: Commit**

```bash
git add docs/site/content/docs/reference/api.mdx
git commit -m "docs(site): add API reference index linking to pkg.go.dev"
```

### Task 2.10: Port `docs/notes/squeezeplay-comparison.md` → `concepts/squeezeplay-comparison.mdx`

**Files:**
- Create: `docs/site/content/docs/concepts/squeezeplay-comparison.mdx`
- Delete: `docs/notes/squeezeplay-comparison.md`

- [ ] **Step 1: Lift the existing file to the new location**

```bash
cp docs/notes/squeezeplay-comparison.md docs/site/content/docs/concepts/squeezeplay-comparison.mdx
```

- [ ] **Step 2: Add Fumadocs frontmatter**

Open `docs/site/content/docs/concepts/squeezeplay-comparison.mdx`; insert at the top:

```mdx
---
title: Squeezeplay UDAP cross-check
description: How go-udap's UDAP implementation compares to ralph-irving/squeezeplay
---

```

Replace the existing `# Squeezeplay UDAP cross-check note` H1 with nothing — the frontmatter handles the title.

- [ ] **Step 3: Update internal references**

The original note may reference `docs/superpowers/plans/...` paths.
Those paths are internal-process docs and stay where they are; the
references should still work as relative-to-repo-root links. If
Fumadocs needs absolute URLs, replace with `https://github.com/yo61/go-udap/blob/main/...`
form.

- [ ] **Step 4: Delete the original**

```bash
git rm docs/notes/squeezeplay-comparison.md
```

- [ ] **Step 5: If `docs/notes/` is now empty, remove it**

```bash
rmdir docs/notes 2>/dev/null || true
```

- [ ] **Step 6: Verify renders**

`pnpm dev`; visit `/docs/concepts/squeezeplay-comparison`.

- [ ] **Step 7: Commit**

```bash
git add docs/site/content/docs/concepts/squeezeplay-comparison.mdx
git commit -m "docs(site): move squeezeplay comparison to site concepts"
```

### Task 2.11: Port `docs/playbooks/sbr-capture.md` → `how-to/capture-wire-traces.mdx`

**Files:**
- Create: `docs/site/content/docs/how-to/capture-wire-traces.mdx`
- Delete: `docs/playbooks/sbr-capture.md`

- [ ] **Step 1: Lift content with frontmatter**

```bash
cp docs/playbooks/sbr-capture.md docs/site/content/docs/how-to/capture-wire-traces.mdx
```

Edit to add frontmatter:

```mdx
---
title: Capture UDAP wire traces from a real SBR
description: Operational playbook for tcpdump-based capture sessions
---

```

Remove the existing `# Squeezebox Receiver capture playbook` H1 to avoid duplication.

The page is structurally a how-to (operational recipe). It doesn't
yet use the `<HowTo>` MDX component — that's added in M3 when the
component lands. For M2 it stays as plain MDX with the same
structure as it had as plain Markdown.

- [ ] **Step 2: Delete the original and the directory if empty**

```bash
git rm docs/playbooks/sbr-capture.md
rmdir docs/playbooks 2>/dev/null || true
```

- [ ] **Step 3: Verify and commit**

```bash
pnpm dev
# spot-check /docs/how-to/capture-wire-traces

git add docs/site/content/docs/how-to/capture-wire-traces.mdx
git commit -m "docs(site): move SBR capture playbook to site how-to"
```

### Task 2.12: Split README §Troubleshooting

**Files:**
- Create: `docs/site/content/docs/how-to/no-devices-found.mdx`
- Create: `docs/site/content/docs/how-to/handle-permission-errors.mdx`
- (Content for `concepts/how-udap-discovery-works.mdx` and `concepts/multi-nic-discovery.mdx` is fully written in M3; this task adds NO content for those.)

The README's Troubleshooting section blends actionable recipes
(how-to) with conceptual explanation. Split the actionable parts now;
the conceptual parts will fold into M3's net-new concept pages.

- [ ] **Step 1: Write `how-to/no-devices-found.mdx`**

```mdx
---
title: Troubleshoot "no devices found"
description: Diagnostic steps when go-udap discover returns nothing
---

# "No devices found"

When `go-udap discover` returns nothing, work through this checklist
in order.

## Step 1: Confirm the device is powered on

The most boring cause. Plug it in, wait 30 seconds for it to come
fully up, try again.

## Step 2: Confirm the device is on the same network segment

UDAP discovery is broadcast-based; broadcasts don't cross routers.
The device and your dev machine need to be on the same VLAN / subnet.

If the device is unconfigured (no DHCP lease), it broadcasts from
source IP `0.0.0.0` — it doesn't *have* a subnet yet, but it must be
physically on the same Layer-2 segment.

## Step 3: Check UDP port 17784 isn't blocked

Some host firewalls block inbound UDP on non-standard ports. Test
with tcpdump:

```bash
sudo tcpdump -i any -n -nn 'udp port 17784'
```

In another terminal, run `go-udap discover`. You should see the
outbound broadcast (length 27) and the device's reply (length 61+).

If you see the outbound but no reply, the device isn't responding
(check Step 1 and Step 2 again). If you see neither, your host
firewall is dropping the outbound — find and fix it.

## Step 4: On a multi-homed laptop, try `--interface` or `--all-interfaces`

If your host has Wi-Fi + Ethernet both up, default discovery only
emits on the kernel's default-route NIC. If the device is on the
*other* NIC's subnet, you'll see zero devices.

See [How to discover on a multi-NIC laptop](./discover-on-multi-nic-laptop).
```

- [ ] **Step 2: Write `how-to/handle-permission-errors.mdx`**

```mdx
---
title: Handle permission errors
description: When binding UDP port 17784 needs elevated privileges
---

# Permission errors

UDP port 17784 is above 1024, so binding it typically does NOT need
root. On some platforms (rare), the port may be reserved or
otherwise restricted, in which case you'll see an error like:

```text
listen UDP: listen udp4 0.0.0.0:17784: bind: permission denied
```

If that happens:

- On macOS: usually a corporate-MDM or LittleSnitch-style rule. Check
  your firewall config.
- On Linux: rare but possible. Try `sudo go-udap discover` once to
  confirm; if that works, find what restricts unprivileged 17784.
- On Windows: check Windows Defender Firewall for go-udap.exe.

If `sudo` does NOT fix it, the next thing to check is whether some
other process is already bound to 17784:

```bash
# Linux
sudo ss -ulnp | grep :17784

# macOS
sudo lsof -iUDP:17784
```

If `mocksbr` (the test mock) is running on the same host, that
explains it — stop the mock.
```

- [ ] **Step 3: Verify renders and commit**

```bash
pnpm dev   # spot-check both pages
git add docs/site/content/docs/how-to
git commit -m "docs(site): split README Troubleshooting into how-tos"
```

### Task 2.13: Port DEVELOPMENT.md → `contributing/`

**Files:**
- Create: `docs/site/content/docs/contributing/building-from-source.mdx`
- Create: `docs/site/content/docs/contributing/cross-compilation.mdx`
- Create: `docs/site/content/docs/contributing/release-process.mdx`
- Delete: `DEVELOPMENT.md`

- [ ] **Step 1: Inventory DEVELOPMENT.md sections**

```bash
grep -n '^##\s' DEVELOPMENT.md
```

Note each top-level section. Distribute as follows:
- Prerequisites + Building from Source → `contributing/building-from-source.mdx`
- Cross-Compilation → `contributing/cross-compilation.mdx`
- Release process / Taskfile commands → `contributing/release-process.mdx`

If DEVELOPMENT.md has additional sections (testing, linting, etc.),
distribute by content fit — testing fits in `building-from-source`,
linting too.

- [ ] **Step 2: Write `building-from-source.mdx`**

Lift content from DEVELOPMENT.md §Prerequisites and §Building from Source. Add frontmatter:

```mdx
---
title: Building from source
description: Build, test, and lint go-udap locally
---

# Building from source

## Prerequisites

- Go 1.26 or later (see `go.mod`)
- [Task](https://taskfile.dev/) (optional, for build automation)
- [prek](https://github.com/j178/prek) (optional, for pre-commit hooks)

## Build

(Lift the Task and Manual Commands blocks from DEVELOPMENT.md.)

## Test

(Lift the test commands from DEVELOPMENT.md.)

## Lint

(If DEVELOPMENT.md covers linting, lift here.)
```

The implementer should fill in the bracketed sections with the
actual content from `DEVELOPMENT.md`.

- [ ] **Step 3: Write `cross-compilation.mdx`**

```mdx
---
title: Cross-compilation
description: Building go-udap for other platforms
---

# Cross-compilation

The tool uses pure Go networking with no cgo dependencies — every
target compiles without a cross-compile toolchain.

(Lift the platform-table and cross-compile commands from
DEVELOPMENT.md.)
```

- [ ] **Step 4: Write `release-process.mdx`**

```mdx
---
title: Release process
description: How releases are cut and published
---

# Release process

Releases are automated via
[semantic-release](https://github.com/semantic-release/semantic-release):
every merge to `main` whose squash-commit prefix is `feat:` or
`fix:` triggers a version bump.

(See Task #35 in the project tracker for the planned switch to
release-please.)

(Lift any release-process content from DEVELOPMENT.md. If
DEVELOPMENT.md doesn't cover this, the page can be shorter — link to
the GitHub Releases page.)
```

- [ ] **Step 5: Delete DEVELOPMENT.md**

```bash
git rm DEVELOPMENT.md
```

- [ ] **Step 6: Verify renders and commit**

```bash
pnpm dev
git add docs/site/content/docs/contributing
git commit -m "docs(site): port DEVELOPMENT.md to site contributing area"
```

### Task 2.14: Reduce README.md to thin landing page

**Files:**
- Modify: `README.md` (rewrite from scratch)

After M2, the canonical user docs live at https://yo61.github.io/go-udap/.
The README becomes a thin landing page pointing there.

- [ ] **Step 1: Rewrite the README**

Replace the entire contents of `README.md` with:

```markdown
# go-udap

[![CI](https://github.com/yo61/go-udap/actions/workflows/ci.yml/badge.svg)](https://github.com/yo61/go-udap/actions/workflows/ci.yml)
[![Docs](https://github.com/yo61/go-udap/actions/workflows/docs.yml/badge.svg)](https://yo61.github.io/go-udap/)
[![Release](https://img.shields.io/github/v/release/yo61/go-udap)](https://github.com/yo61/go-udap/releases)

A command-line tool for discovering and configuring Squeezebox devices
on your network using the UDAP (Universal Device Access Protocol).

## 📖 Documentation

**Full documentation lives at [yo61.github.io/go-udap](https://yo61.github.io/go-udap/)**.

Quick links:
- [Tutorial: configure your first Squeezebox](https://yo61.github.io/go-udap/docs/tutorials/configure-your-first-squeezebox)
- [How-to guides](https://yo61.github.io/go-udap/docs/how-to)
- [Command reference](https://yo61.github.io/go-udap/docs/reference/commands)
- [Concepts](https://yo61.github.io/go-udap/docs/concepts)
- [API reference (pkg.go.dev)](https://pkg.go.dev/go-udap)

## Install

```bash
# Homebrew
brew install yo61/tap/go-udap

# Or download from the Releases page:
# https://github.com/yo61/go-udap/releases
```

See [installation guide](https://yo61.github.io/go-udap/docs/how-to/install-go-udap)
for all options.

## Quick start

```bash
go-udap discover                          # find devices on the LAN
go-udap info 00:04:20:16:06:02            # show one device's metadata
go-udap getip 00:04:20:16:06:02           # check its IP / subnet / gateway
go-udap set 00:04:20:16:06:02 \
  --hostname living-room --reboot         # rename and reboot
```

The [tutorial](https://yo61.github.io/go-udap/docs/tutorials/configure-your-first-squeezebox)
walks through a complete first-time setup.

## Contributing

See [contributing docs](https://yo61.github.io/go-udap/docs/contributing).

## License

MIT — see [LICENSE](LICENSE).
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: reduce README to landing page pointing at docs site"
```

### Task 2.15: Push and open the M2 PR

- [ ] **Step 1: Run a clean build**

```bash
cd docs/site
pnpm install --frozen-lockfile
pnpm build
```

Verify the build still passes after all the new content.

- [ ] **Step 2: Push and open PR**

```bash
git push -u origin feat/docs-site-m2-content
gh pr create --title "feat(docs): port existing docs into Diataxis quadrants" --body "Closes M2 of Task #28. Lifts every existing piece of user-facing docs into the new Fumadocs site under Diataxis-shaped sections.

## Changes
- Created section landings for Tutorials / How-to / Reference / Concepts / Contributing
- Ported README sections: Overview → concepts/, Installation → how-to/, Usage/Commands → reference/commands/, Config format + Parameters → reference/, Troubleshooting → split into how-tos
- Ported DEVELOPMENT.md → contributing/
- Ported docs/notes/squeezeplay-comparison.md and docs/playbooks/sbr-capture.md
- Reduced README.md to thin landing page pointing at the published site
- Deleted DEVELOPMENT.md, docs/notes/squeezeplay-comparison.md, docs/playbooks/sbr-capture.md

## Test plan
- [x] pnpm build clean
- [x] All ported pages render in pnpm dev
- [x] Internal links resolve
- [ ] After merge: published site shows all sections populated"
```

---

## Milestone M3 — Net-new content

**Branch:** `feat/docs-site-m3-content`. Branched from `main` after M2 merges.
**PR title:** `feat(docs): tutorial, how-to template, and net-new concept pages`

### Task 3.1: Build the `<HowTo>` MDX component

**Files:**
- Create: `docs/site/components/how-to.tsx`
- Modify: `docs/site/mdx-components.tsx` (or whatever Fumadocs uses to register MDX components — exact filename per Fumadocs convention)

- [ ] **Step 1: Locate Fumadocs's MDX-components registry**

```bash
ls docs/site/mdx-components.tsx docs/site/source.config.ts
```

Fumadocs typically uses `mdx-components.tsx` at the project root to register components available inside MDX. Check what's there.

- [ ] **Step 2: Write the component**

Create `docs/site/components/how-to.tsx`:

```typescript
import type { ReactNode } from 'react';

export interface HowToProps {
  goal: string;
  prerequisites: string[];
  steps: ReactNode[];
  verification: string | ReactNode;
  exampleFile?: string;
}

export function HowTo({
  goal,
  prerequisites,
  steps,
  verification,
  exampleFile,
}: HowToProps) {
  return (
    <div className="not-prose flex flex-col gap-6 my-6">
      <section className="rounded-lg border border-fd-border bg-fd-card p-5">
        <h2 className="text-sm font-semibold uppercase tracking-wider text-fd-muted-foreground mb-2">
          Goal
        </h2>
        <p className="text-base text-fd-foreground">{goal}</p>
      </section>

      <section className="rounded-lg border border-fd-border bg-fd-card p-5">
        <h2 className="text-sm font-semibold uppercase tracking-wider text-fd-muted-foreground mb-3">
          Prerequisites
        </h2>
        <ul className="list-disc list-inside space-y-1 text-fd-foreground">
          {prerequisites.map((p, i) => (
            <li key={i}>{p}</li>
          ))}
        </ul>
      </section>

      <section className="rounded-lg border border-fd-border bg-fd-card p-5">
        <h2 className="text-sm font-semibold uppercase tracking-wider text-fd-muted-foreground mb-3">
          Steps
        </h2>
        <ol className="list-decimal list-inside space-y-2 text-fd-foreground">
          {steps.map((s, i) => (
            <li key={i} className="leading-relaxed">{s}</li>
          ))}
        </ol>
      </section>

      <section className="rounded-lg border border-fd-border bg-fd-card p-5">
        <h2 className="text-sm font-semibold uppercase tracking-wider text-fd-muted-foreground mb-2">
          Verification
        </h2>
        <div className="text-fd-foreground">{verification}</div>
      </section>

      {exampleFile && (
        <section className="rounded-lg border border-fd-border bg-fd-card p-5">
          <h2 className="text-sm font-semibold uppercase tracking-wider text-fd-muted-foreground mb-2">
            Example file
          </h2>
          <a
            href={exampleFile}
            download
            className="inline-flex items-center gap-2 text-fd-foreground underline hover:no-underline"
          >
            Download <code className="text-sm">{exampleFile.split('/').pop()}</code>
          </a>
        </section>
      )}
    </div>
  );
}
```

- [ ] **Step 3: Register the component in `mdx-components.tsx`**

Open `docs/site/mdx-components.tsx`; the existing content will look something like:

```typescript
import defaultComponents from 'fumadocs-ui/mdx';
import type { MDXComponents } from 'mdx/types';

export function getMDXComponents(components?: MDXComponents): MDXComponents {
  return {
    ...defaultComponents,
    ...components,
  };
}
```

Modify to import and expose `HowTo`:

```typescript
import defaultComponents from 'fumadocs-ui/mdx';
import type { MDXComponents } from 'mdx/types';
import { HowTo } from './components/how-to';

export function getMDXComponents(components?: MDXComponents): MDXComponents {
  return {
    ...defaultComponents,
    HowTo,
    ...components,
  };
}
```

- [ ] **Step 4: Verify with a smoke-test MDX page**

Create a temporary `docs/site/content/docs/_howto-smoke.mdx`:

```mdx
---
title: HowTo component smoke test
---

<HowTo
  goal="Verify the HowTo component renders"
  prerequisites={["Visit this page"]}
  steps={["Read the rendered output"]}
  verification="Five labelled sections appear above (Goal, Prerequisites, Steps, Verification, Example file optional)"
  exampleFile="/examples/dhcp.conf"
/>
```

Run `pnpm dev`; visit `/docs/_howto-smoke`. All five sections render with consistent styling.

- [ ] **Step 5: Remove the smoke-test file**

```bash
rm docs/site/content/docs/_howto-smoke.mdx
```

- [ ] **Step 6: Commit**

```bash
git add docs/site/components/how-to.tsx docs/site/mdx-components.tsx
git commit -m "feat(docs-site): add <HowTo> MDX structural component"
```

### Task 3.2: Write the tutorial

**Files:**
- Create: `docs/site/content/docs/tutorials/configure-your-first-squeezebox.mdx`

- [ ] **Step 1: Write the tutorial page**

```mdx
---
title: Configure your first Squeezebox in 5 minutes
description: A complete walkthrough from factory state to a working device
---

# Configure your first Squeezebox in 5 minutes

This tutorial walks you through configuring a brand-new (or
factory-reset) Squeezebox from the command line. By the end, you'll
have a device with a hostname you chose, on DHCP, pointed at your
Lyrion Music Server.

## What you need

- A Squeezebox device (Receiver, Boom, Touch, Radio — any model works)
- An Ethernet cable connecting the device to the same LAN as your
  computer (Wi-Fi setup is a separate how-to; for first onboarding,
  wired is much simpler)
- `go-udap` [installed](/docs/how-to/install-go-udap)
- A Lyrion Music Server running somewhere on your LAN (note its IP
  address)

## Step 1 — Put the device in setup mode

If the device is brand-new out of the box, it's already in setup mode
— skip to Step 2.

If you're configuring an already-configured device, hold the front
button for 3-6 seconds until the light flashes red. (Holding for 6+
seconds triggers a full factory reset; you don't need that for
just-reconfigure.)

## Step 2 — Discover the device

```bash
go-udap discover
```

Expected output (one MAC, your device's):

```text
00:04:20:16:06:02
```

If you see nothing, see
[Troubleshoot "no devices found"](/docs/how-to/no-devices-found).

## Step 3 — Look at what it currently reports

```bash
go-udap info 00:04:20:16:06:02
```

Replace the MAC with what `discover` showed you. Expected:

```text
MAC:      00:04:20:16:06:02
IP:       0.0.0.0
Name:     Squeezebox Device
Model:    Squeezebox Receiver
Firmware: 77
HW Rev:   0005
State:    init
```

The `0.0.0.0` IP and `init` state confirm the device is unconfigured.

## Step 4 — Apply your configuration and reboot

Choose a hostname for your device (e.g. `living-room`) and find the
IP of your Lyrion Music Server (e.g. `192.168.1.250`).

```bash
go-udap set 00:04:20:16:06:02 \
  --hostname living-room \
  --lan-ip-mode 1 \
  --server-address 192.168.1.250 \
  --reboot
```

- `--lan-ip-mode 1` = DHCP
- `--server-address` points the device at your music server
- `--reboot` reboots after writing — necessary for the new config to
  take effect

Expected: command returns within a couple of seconds. The device
reboots and is unreachable for ~10 seconds.

## Step 5 — Verify

Wait ~15 seconds for the device to come back up, then:

```bash
go-udap discover --info
```

Expected: the device now reports its DHCP-assigned IP, its new
hostname, and `State: connected`.

```text
MAC:      00:04:20:16:06:02
IP:       192.168.1.78
Name:     living-room
Model:    Squeezebox Receiver
Firmware: 77
HW Rev:   0005
State:    connected
IP:      192.168.1.78
Subnet:  255.255.255.0
Gateway: 192.168.1.1
```

The `IP:` / `Subnet:` / `Gateway:` lines come from a `get_ip` query
that `--info` fires per device — they confirm the device's own view
of its network state.

## What's next?

- Want to configure over Wi-Fi instead of Ethernet? See
  [Configure a Squeezebox for Wi-Fi with WPA2](/docs/how-to/configure-wifi-wpa2).
- Want to back up the config? See
  [Back up and restore a device config](/docs/how-to/install-go-udap)
  (uses `read` and `set --config`).
- Want to understand what's happening on the wire? See
  [How UDAP discovery works](/docs/concepts/how-udap-discovery-works).
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev   # spot-check /docs/tutorials/configure-your-first-squeezebox
git add docs/site/content/docs/tutorials/configure-your-first-squeezebox.mdx
git commit -m "docs(site): add 5-minute first-Squeezebox tutorial"
```

### Task 3.3: Create example config files

**Files:**
- Create: `docs/site/public/examples/dhcp.conf`
- Create: `docs/site/public/examples/static-ip.conf`
- Create: `docs/site/public/examples/wifi-wpa2.conf`

Fumadocs serves files in `public/` at the site root, so
`/go-udap/examples/dhcp.conf` will resolve.

- [ ] **Step 1: Write `dhcp.conf`**

```ini
# DHCP on wired Ethernet — minimal example
# Use with: go-udap set <mac> --config dhcp.conf --reboot
interface=1
lan_ip_mode=1

# Set your music server's IP:
server_address=192.168.1.250
```

- [ ] **Step 2: Write `static-ip.conf`**

```ini
# Static IP on wired Ethernet
# Use with: go-udap set <mac> --config static-ip.conf --reboot
# Edit the network addresses below to match your subnet.
interface=1
lan_ip_mode=0
lan_network_address=192.168.1.50
lan_subnet_mask=255.255.255.0
lan_gateway=192.168.1.1
primary_dns=192.168.1.1
secondary_dns=8.8.8.8

# Set your music server's IP:
server_address=192.168.1.250
```

- [ ] **Step 3: Write `wifi-wpa2.conf`**

```ini
# Wi-Fi (WPA2) on DHCP
# Use with: go-udap set <mac> --config wifi-wpa2.conf --reboot
# Edit the SSID and pre-shared key to match your network.
interface=0
lan_ip_mode=1
wireless_SSID=YourNetworkName
wireless_wpa_on=1
wireless_wpa_mode=2
wireless_wpa_cipher=2
wireless_wpa_psk=your-pre-shared-key

# Set your music server's IP:
server_address=192.168.1.250
```

- [ ] **Step 4: Verify the files are served**

`pnpm dev`; visit `http://localhost:3000/examples/dhcp.conf`. Expected: the file's text content is returned.

- [ ] **Step 5: Commit**

```bash
git add docs/site/public/examples
git commit -m "docs(site): add downloadable example config files"
```

### Task 3.4: Write `how-to/configure-for-dhcp.mdx`

**Files:**
- Create: `docs/site/content/docs/how-to/configure-for-dhcp.mdx`

- [ ] **Step 1: Write the page using the `<HowTo>` component**

```mdx
---
title: Configure a Squeezebox for DHCP
description: Wired Ethernet, DHCP-assigned IP
---

# Configure a Squeezebox for DHCP

<HowTo
  goal="Configure a Squeezebox to use DHCP on wired Ethernet, pointed at your Lyrion Music Server"
  prerequisites={[
    "Device connected by Ethernet to the same subnet as the dev machine",
    "Device in setup or init mode (front-button hold 3-6s if needed; new devices are already in setup mode)",
    "go-udap installed and on PATH",
    "Your Lyrion Music Server's IP address known (e.g. 192.168.1.250)"
  ]}
  steps={[
    <>Download the <a href="/go-udap/examples/dhcp.conf">example config</a> and edit <code>server_address</code> to your LMS IP.</>,
    <>Discover the device: <code>go-udap discover</code></>,
    <>Apply the config and reboot: <code>go-udap set &lt;mac&gt; --config dhcp.conf --reboot</code></>,
  ]}
  verification={
    <>After ~15 seconds, run <code>go-udap getip &lt;mac&gt;</code>. You should see a DHCP-assigned IP from your network's subnet.</>
  }
  exampleFile="/go-udap/examples/dhcp.conf"
/>

## Notes

- `lan_ip_mode=1` is DHCP; `lan_ip_mode=0` is static (see the static IP
  how-to for that).
- `interface=1` selects wired Ethernet. `interface=0` selects wireless.
- The `--reboot` flag is necessary; network config changes don't take
  effect until the device reboots.
```

- [ ] **Step 2: Verify renders and commit**

```bash
pnpm dev
git add docs/site/content/docs/how-to/configure-for-dhcp.mdx
git commit -m "docs(site): add how-to for DHCP configuration"
```

### Task 3.5: Write `how-to/configure-for-static-ip.mdx`

**Files:**
- Create: `docs/site/content/docs/how-to/configure-for-static-ip.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Configure a Squeezebox for static IP
description: Wired Ethernet with a fixed IP address
---

# Configure a Squeezebox for static IP

<HowTo
  goal="Configure a Squeezebox to use a fixed IP address on wired Ethernet"
  prerequisites={[
    "Device connected by Ethernet to the same subnet as the dev machine",
    "Device in setup or init mode",
    "go-udap installed and on PATH",
    "Static IP details known: target IP, subnet mask, gateway, DNS",
    "Your Lyrion Music Server's IP address known"
  ]}
  steps={[
    <>Download the <a href="/go-udap/examples/static-ip.conf">example config</a> and edit the IP / subnet / gateway / DNS / LMS values to match your network.</>,
    <>Discover the device: <code>go-udap discover</code></>,
    <>Apply: <code>go-udap set &lt;mac&gt; --config static-ip.conf --reboot</code></>,
  ]}
  verification={
    <>After ~15 seconds, run <code>go-udap getip &lt;mac&gt;</code>. The IP / Subnet / Gateway should match exactly what you put in the config.</>
  }
  exampleFile="/go-udap/examples/static-ip.conf"
/>

## Notes

- `lan_ip_mode=0` selects static IP. The four fields
  (`lan_network_address`, `lan_subnet_mask`, `lan_gateway`,
  `primary_dns`) become authoritative.
- Test the chosen IP isn't already in use on your network first
  (otherwise the device will appear to work but cause arp conflicts).
- If the device doesn't come back after reboot, the IP you chose
  isn't reachable. Factory-reset by holding the front button for 6+
  seconds and try again with a different IP.
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/how-to/configure-for-static-ip.mdx
git commit -m "docs(site): add how-to for static IP configuration"
```

### Task 3.6: Write `how-to/configure-wifi-wpa2.mdx`

**Files:**
- Create: `docs/site/content/docs/how-to/configure-wifi-wpa2.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Configure a Squeezebox for Wi-Fi with WPA2
description: Wireless networking with WPA2-PSK authentication
---

# Configure a Squeezebox for Wi-Fi with WPA2

<HowTo
  goal="Configure a Squeezebox to use Wi-Fi with WPA2-PSK"
  prerequisites={[
    "Device connected by Ethernet for the initial setup (Wi-Fi-first onboarding is tracked separately, see below)",
    "Device in setup or init mode",
    "go-udap installed and on PATH",
    "Your Wi-Fi SSID and pre-shared key known",
    "Your Lyrion Music Server's IP address known"
  ]}
  steps={[
    <>Download the <a href="/go-udap/examples/wifi-wpa2.conf">example config</a> and edit <code>wireless_SSID</code>, <code>wireless_wpa_psk</code>, and <code>server_address</code>.</>,
    <>Discover the device (still over Ethernet): <code>go-udap discover</code></>,
    <>Apply: <code>go-udap set &lt;mac&gt; --config wifi-wpa2.conf --reboot</code></>,
    <>Unplug the Ethernet cable.</>,
  ]}
  verification={
    <>After ~30 seconds (Wi-Fi association takes longer than wired), re-run <code>go-udap discover</code>. The device should appear with its DHCP-assigned Wi-Fi IP. If it doesn't, plug the Ethernet back in to recover and check the SSID/PSK values.</>
  }
  exampleFile="/go-udap/examples/wifi-wpa2.conf"
/>

## Notes

- `interface=0` selects wireless. Without this, the device stays on
  Ethernet regardless of any wireless settings.
- `wireless_wpa_mode=2` is WPA2; `wireless_wpa_mode=1` is WPA;
  `wireless_wpa_mode=0` is WPA-mixed-mode.
- WEP is also supported (`wireless_wep_on=1`, `wireless_wep_key=...`)
  but is insecure; don't use it on networks you care about.

## Wi-Fi-first onboarding (without Ethernet)

Initial onboarding of a factory-state device over Wi-Fi (using the
device's built-in ad-hoc network) is a separate procedure. See
[Onboard over Wi-Fi using ad-hoc mode](./onboard-over-wifi-ad-hoc).
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/how-to/configure-wifi-wpa2.mdx
git commit -m "docs(site): add how-to for Wi-Fi WPA2 configuration"
```

### Task 3.7: Write `how-to/discover-on-multi-nic-laptop.mdx`

**Files:**
- Create: `docs/site/content/docs/how-to/discover-on-multi-nic-laptop.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Discover on a multi-NIC laptop
description: Use --interface or --all-interfaces when your dev machine has multiple network interfaces
---

# Discover on a multi-NIC laptop

If your laptop has Wi-Fi + wired Ethernet both up, default
`go-udap discover` only emits broadcasts on the kernel's
default-route NIC. If your Squeezebox is on the *other* interface's
subnet, you'll see zero devices.

<HowTo
  goal="Make go-udap discover find a Squeezebox that's reachable only via a non-default NIC"
  prerequisites={[
    "go-udap installed and on PATH",
    "Multi-NIC dev machine (Wi-Fi + Ethernet, or multiple Ethernet)",
    "macOS or Linux — Windows multi-NIC discovery is not yet supported (tracked as Task #29)"
  ]}
  steps={[
    <>List your usable interfaces: <code>go-udap interfaces</code></>,
    <>Either pick one explicitly: <code>go-udap --interface en7 discover</code></>,
    <>Or fan out across all of them: <code>go-udap --all-interfaces discover</code></>,
  ]}
  verification={
    <>You should now see the device's MAC in the output. Run <code>tcpdump -i any 'udp port 17784'</code> in another terminal to confirm the broadcast went out the NIC you expected.</>
  }
/>

## Which one should I use?

- **`--interface NAME`** — when you know exactly which NIC reaches
  the device. Faster (one socket), simpler.
- **`--all-interfaces`** — when you're unsure. Sends concurrently on
  every interface; the device replies via whichever one reaches it.

The two flags are mutually exclusive. Combining them is a usage error
(exit code 1).

## What's actually happening on the wire?

See [Multi-NIC discovery](/docs/concepts/multi-nic-discovery) for the
underlying mechanics (`IP_BOUND_IF`, `SO_BINDTODEVICE`,
`SO_REUSEPORT`).
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/how-to/discover-on-multi-nic-laptop.mdx
git commit -m "docs(site): add how-to for multi-NIC discovery"
```

### Task 3.8: Write `how-to/recover-stuck-in-init-mode.mdx`

**Files:**
- Create: `docs/site/content/docs/how-to/recover-stuck-in-init-mode.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Recover a Squeezebox stuck in init mode
description: When a device won't leave init/setup state
---

# Recover a Squeezebox stuck in init mode

A Squeezebox reports `State: init` when it hasn't yet completed
initial configuration. If a previous `go-udap set` was interrupted or
applied bad values, the device can sit in `init` indefinitely.

<HowTo
  goal="Get a Squeezebox out of init state and into connected state"
  prerequisites={[
    "Physical access to the device",
    "go-udap installed",
    "Device connected to power and on the same LAN as the dev machine"
  ]}
  steps={[
    <>Confirm the device is reachable: <code>go-udap discover</code></>,
    <>If the device is in init state and you want to start over: factory-reset by holding the front button for 6+ seconds. The light flashes rapidly red, then the device reboots into a clean factory state.</>,
    <>Re-run the <a href="/go-udap/docs/tutorials/configure-your-first-squeezebox">tutorial</a> from a clean state.</>,
    <>If the device DOESN'T appear in <code>discover</code> after factory reset, see <a href="/go-udap/docs/how-to/no-devices-found">Troubleshoot &quot;no devices found&quot;</a>.</>,
  ]}
  verification={
    <>After the tutorial completes successfully, <code>go-udap info &lt;mac&gt;</code> should show <code>State: connected</code> and a real DHCP IP (not 0.0.0.0).</>
  }
/>

## Why does this happen?

`init` is the device's default state until something — typically the
server-association handshake — confirms the device has joined a music
server. If `server_address` was set to an unreachable IP, the device
stays in init forever waiting.

Factory reset is the safe recovery: it wipes NVRAM back to defaults
and starts the setup state machine over.
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/how-to/recover-stuck-in-init-mode.mdx
git commit -m "docs(site): add how-to for stuck-in-init recovery"
```

### Task 3.9: Write `how-to/onboard-over-wifi-ad-hoc.mdx` (stub)

**Files:**
- Create: `docs/site/content/docs/how-to/onboard-over-wifi-ad-hoc.mdx`

This is a STUB — the actual procedure depends on Task #34's outcome.

- [ ] **Step 1: Write the stub**

```mdx
---
title: Onboard a Squeezebox over Wi-Fi (ad-hoc)
description: Configure a factory-state device without plugging in Ethernet
---

# Onboard a Squeezebox over Wi-Fi (ad-hoc)

> **This page is a placeholder.** The procedure for onboarding a
> factory-state Squeezebox over its built-in ad-hoc Wi-Fi is being
> investigated as
> [Task #34](https://github.com/yo61/go-udap/issues?q=is%3Aopen+ad-hoc+wifi)
> in the project tracker. Real content will appear here once the
> investigation completes.

## What we know so far

Squeezebox firmware exposes an ad-hoc Wi-Fi network when in factory
state. Squeezeplay's reference setup applet associates with that
network and runs UDAP discovery over Wi-Fi without needing Ethernet
first. We expect go-udap to be able to do the same with `--retries 2`
(matching squeezeplay's hardcoded triple-send) once we've verified
the ad-hoc SSID pattern, security model, and modern OS support.

## In the meantime

Use wired Ethernet for initial onboarding —
[Configure a Squeezebox for DHCP](./configure-for-dhcp) then switch
to Wi-Fi with
[Configure a Squeezebox for Wi-Fi with WPA2](./configure-wifi-wpa2).
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/how-to/onboard-over-wifi-ad-hoc.mdx
git commit -m "docs(site): add stub for ad-hoc Wi-Fi onboarding (Task #34)"
```

### Task 3.10: Write `concepts/how-udap-discovery-works.mdx`

**Files:**
- Create: `docs/site/content/docs/concepts/how-udap-discovery-works.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: How UDAP discovery works
description: The wire-level mechanics of finding devices
---

# How UDAP discovery works

## The discovery exchange

When you run `go-udap discover`, three things happen:

1. **Outbound request.** go-udap binds a UDP socket to
   `0.0.0.0:17784` (or to a specific interface if `--interface` is set)
   and sends a 27-byte UDAP advanced-discovery packet (UCP method
   `0x0009`) to **`255.255.255.255:17784`**. The destination is
   limited broadcast — see "Why limited broadcast?" below.

2. **Device replies.** Every Squeezebox on the same Layer-2 segment
   that has UDAP enabled responds with its own UDAP packet containing
   metadata TLVs (name, model, firmware, hardware revision, device
   status, UUID).

3. **Listener loops.** go-udap reads from the same socket until the
   timeout expires (default 5s), collecting reply packets and parsing
   them into the device list.

## Why limited broadcast (`255.255.255.255`)?

A *factory-state* Squeezebox has no IP address. Its source IP in any
discovery reply is `0.0.0.0`. Because it doesn't know its own
subnet, it can't recognise a directed broadcast like
`192.168.1.255` as "for it" — those are subnet-scoped, and the
device hasn't joined a subnet yet.

Limited broadcast `255.255.255.255` is the only address an
un-numbered device will process as "me." So go-udap always sends to
`255.255.255.255`, even when it knows the LAN's directed broadcast
address (you'll see the directed one in `go-udap interfaces`'
`BROADCAST` column — informational only).

This was discovered the hard way during Phase 6 hardware testing
(see
[Squeezeplay UDAP cross-check](./squeezeplay-comparison)
for the post-mortem).

## The UUID fallback

Modern firmware includes a UUID TLV (`0x0d`) in the discovery reply.
Older firmware doesn't. When the discovery reply omits UUID, go-udap
falls back to a separate `get_uuid` (UCP method `0x000b`) query to
populate the field. Visible in `info` and `discover --info` output.

The fallback is silent — if it fails (device doesn't respond to
`get_uuid` either), the UUID line is just omitted. Pass `--verbose`
to see the diagnostic.

## The retry knob

For wired LAN, a single broadcast send is reliable. For ad-hoc Wi-Fi
during initial onboarding (very lossy), the squeezeplay reference
implementation hardcodes a triple-send pattern. go-udap exposes this
as `--retries N`: N additional re-transmits after the initial send,
no inter-send delay. `--retries 2` matches squeezeplay's behaviour.

## See also

- [Multi-NIC discovery](./multi-nic-discovery) — why a multi-homed
  dev box behaves the way it does
- [Squeezeplay UDAP cross-check](./squeezeplay-comparison) — how
  go-udap's protocol handling compares to the Lua reference
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/concepts/how-udap-discovery-works.mdx
git commit -m "docs(site): add concept page for UDAP discovery mechanics"
```

### Task 3.11: Write `concepts/multi-nic-discovery.mdx`

**Files:**
- Create: `docs/site/content/docs/concepts/multi-nic-discovery.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Multi-NIC discovery
description: How --interface and --all-interfaces work under the hood
---

# Multi-NIC discovery

A laptop with Wi-Fi + Ethernet both up has more than one path to
broadcast on. The kernel's default-route NIC wins by default — every
other interface stays silent unless you explicitly tell go-udap
otherwise. This page explains the wire-level details.

## The problem

Default `go-udap discover` binds `0.0.0.0:17784` and writes to
`255.255.255.255:17784`. The kernel chooses which interface to emit
the packet on based on its routing table — typically the
default-route NIC.

If the device is on the *other* NIC's subnet (say, a Squeezebox on
your wired LAN while your Wi-Fi default route points at a tethered
phone), the broadcast never reaches it.

## `--interface NAME`

Forces a specific egress interface. The mechanism is platform-specific:

- **macOS:** `IP_BOUND_IF` socket option (IPPROTO_IP option 25). Takes
  the interface index from `EnumerateInterfaces()`. The kernel routes
  all output through that NIC regardless of route-table preference.
- **Linux:** `SO_BINDTODEVICE` socket option (SOL_SOCKET). Takes the
  interface name string. Same effect as `IP_BOUND_IF`. Requires
  `CAP_NET_RAW` on most distros — usually granted to the binary or
  the user.
- **Windows:** Not yet supported. `IP_UNICAST_IF` exists on Windows
  but its broadcast semantics are undocumented; investigation is
  [tracked as Task #29](https://github.com/yo61/go-udap/issues?q=is%3Aopen+windows+interface).
  Until then, the flag is hidden from `--help` on Windows.

The local socket is bound to `0.0.0.0:17784` regardless of the egress
interface — *not* to the interface's specific IP. Binding to the
specific IP would prevent the socket from receiving limited-broadcast
replies (those only reach `0.0.0.0`-bound sockets). This was a real
bug caught by Phase 6 hardware testing.

## `--all-interfaces`

Fans out across every usable interface. Implementation: one
`UDPTransport` per interface, composed into a `MultiTransport`.

The catch: multiple sockets all binding to `0.0.0.0:17784`
simultaneously requires `SO_REUSEPORT` (in addition to
`SO_REUSEADDR`), set **before** `bind()`. go-udap uses
`net.ListenConfig.Control` to set both options pre-bind via a
platform-specific helper (`setReusePortPreBind`).

On Windows, `SO_REUSEPORT` doesn't exist; `--all-interfaces` is
therefore not supported there.

## What you'd see in tcpdump

`./go-udap --all-interfaces discover` on a Mac with en0 (Wi-Fi,
`192.168.1.118`) and en7 (Ethernet, `192.168.1.226`) both on the
same subnet:

```text
16:58:38.746037 IP 192.168.1.118.17784 > 255.255.255.255.17784: UDP, length 27
16:58:38.746183 IP 192.168.1.226.17784 > 255.255.255.255.17784: UDP, length 27
16:58:38.746539 IP   0.0.0.0.17784 > 255.255.255.255.17784: UDP, length 61   ← device reply
```

Two source IPs on the outbound — confirming fan-out really did go out
both NICs.

## Why not just set IP_MULTICAST_IF?

UDAP discovery uses broadcast, not multicast. The
`IP_MULTICAST_IF` socket option doesn't affect broadcast routing on
any of the three target platforms.

## See also

- [How UDAP discovery works](./how-udap-discovery-works) — the
  broader discovery story
- [How to discover on a multi-NIC laptop](/docs/how-to/discover-on-multi-nic-laptop) —
  the practical recipe
- [Global flags reference](/docs/reference/global-flags) — flag
  documentation
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/concepts/multi-nic-discovery.mdx
git commit -m "docs(site): add concept page for multi-NIC discovery mechanics"
```

### Task 3.12: Write `concepts/the-26-nvram-parameters.mdx`

**Files:**
- Create: `docs/site/content/docs/concepts/the-26-nvram-parameters.mdx`

- [ ] **Step 1: Write the page**

```mdx
---
title: Why 26 NVRAM parameters?
description: How go-udap's parameter table relates to the UDAP protocol
---

# Why 26 NVRAM parameters?

The protocol-level NVRAM region on a Squeezebox is much larger than
the 26 entries go-udap recognises. Why these 26 and not more?

## Net::UDAP is the authority

`go-udap` treats the Perl
[Net::UDAP](https://metacpan.org/dist/Net-UDAP) reference
implementation as the canonical source of truth for the protocol.
Net::UDAP defines a constant table listing each NVRAM offset, its
length, and its canonical name. go-udap's `udap.Parameters` slice
mirrors that table exactly.

The 26 entries cover everything the protocol officially exposes:

- Network configuration (IP mode, IP, subnet, gateway, DNS, hostname)
- Wireless config (SSID, region, WEP/WPA settings)
- Server pointer (LMS address)
- A few miscellany (bridging, interface select, server display name)

The full table is in
[reference: NVRAM parameters](/docs/reference/nvram-parameters).

## Aliases

A small number of parameters have aliases (e.g.
`squeezecenter_address` and `slimserver_address` both refer to the
same NVRAM offset as `lms_address`). Aliases are accepted on input
(in `set --config` or `--<param>` flags) but `read` always emits the
canonical name.

## What about parameters NOT in the table?

A Squeezebox's NVRAM has hundreds of bytes go-udap doesn't recognise
as "parameters." These show up in `go-udap read --all` as
`offset_NNN` entries (where NNN is the byte offset). They're shown
for completeness but you can't `set` them — go-udap will refuse
unknown parameter names.

If you need to write a custom offset, you'd extend
`udap.Parameters` and rebuild the binary. The cross-platform
[Squeezeplay UDAP cross-check](./squeezeplay-comparison) inventoried
squeezeplay's own table and confirmed it's a strict subset of ours,
so we're unlikely to be missing anything the reference Lua
implementation uses.

## Adding a new parameter

If you're working on go-udap itself and want to add a new
NVRAM-parameter:

1. Add an entry to `udap/parameters.go`'s `Parameters` slice
2. Verify the offset against a wire capture from a real device
3. Add it to the
   [NVRAM parameters reference](/docs/reference/nvram-parameters)
4. If the parameter has any aliases (other names that point at the
   same offset), add them to `parameterAliases`
```

- [ ] **Step 2: Verify and commit**

```bash
pnpm dev
git add docs/site/content/docs/concepts/the-26-nvram-parameters.mdx
git commit -m "docs(site): add concept page on the NVRAM parameter table"
```

### Task 3.13: Push and open the M3 PR

- [ ] **Step 1: Final clean build**

```bash
cd docs/site
pnpm install --frozen-lockfile
pnpm build
```

Expected: clean. The build catches any broken internal links by
default in Next.js / Fumadocs.

- [ ] **Step 2: Push and open PR**

```bash
git push -u origin feat/docs-site-m3-content
gh pr create --title "feat(docs): tutorial, how-to template, and net-new concept pages" --body "Closes M3 of Task #28. Lands the user-visible payoff:

## Net-new content
- Tutorial: \`tutorials/configure-your-first-squeezebox.mdx\`
- 6 how-to pages: DHCP, static IP, Wi-Fi WPA2, multi-NIC discovery, stuck-in-init recovery, ad-hoc Wi-Fi (stub for Task #34)
- 3 concept pages: how UDAP discovery works, multi-NIC discovery mechanics, the 26 NVRAM parameters
- 3 downloadable example config files served at /examples/*.conf

## Structural
- New \`<HowTo>\` MDX component for consistent how-to layout
- Wired into Fumadocs's MDX components registry

## Test plan
- [x] pnpm build clean
- [x] Every new page renders in pnpm dev
- [x] Internal links resolve
- [x] Downloadable .conf files serve at /examples/*"
```

---

## Milestone M4 — Polish + versioning snapshot mechanism

**Branch:** `feat/docs-site-m4-versioning`. Branched from `main` after M3 merges.
**PR title:** `feat(docs): custom landing page and versioning snapshot mechanism`

### Task 4.1: Design and build a custom landing page

**Files:**
- Modify: `docs/site/app/(home)/page.tsx`

The placeholder from M1 was a one-paragraph holder. M4 replaces it with
a "modern minimal impact" landing — sharp typography, generous
whitespace, dark-mode-first, a hero + section CTAs.

- [ ] **Step 1: Replace the home page**

Write to `docs/site/app/(home)/page.tsx`:

```typescript
import Link from 'next/link';

export default function HomePage() {
  return (
    <main className="flex flex-1 flex-col">
      <section className="border-b border-fd-border bg-gradient-to-b from-fd-background to-fd-card">
        <div className="mx-auto max-w-5xl px-6 py-24 sm:py-32">
          <div className="text-xs uppercase tracking-widest text-fd-muted-foreground mb-4">
            UDAP configuration tool
          </div>
          <h1 className="text-5xl sm:text-6xl font-bold tracking-tight text-fd-foreground">
            go-udap
          </h1>
          <p className="mt-6 text-xl text-fd-muted-foreground max-w-2xl">
            Discover and configure Squeezebox devices from the command line. One
            static binary, every platform, no daemon.
          </p>
          <div className="mt-10 flex flex-wrap gap-4">
            <Link
              href="/docs/tutorials/configure-your-first-squeezebox"
              className="inline-flex items-center rounded-md bg-fd-foreground px-5 py-2.5 text-sm font-medium text-fd-background hover:opacity-90"
            >
              5-minute tutorial →
            </Link>
            <Link
              href="/docs"
              className="inline-flex items-center rounded-md border border-fd-border px-5 py-2.5 text-sm font-medium text-fd-foreground hover:bg-fd-card"
            >
              Browse the docs
            </Link>
            <a
              href="https://github.com/yo61/go-udap"
              className="inline-flex items-center rounded-md border border-fd-border px-5 py-2.5 text-sm font-medium text-fd-foreground hover:bg-fd-card"
            >
              GitHub
            </a>
          </div>
        </div>
      </section>

      <section className="mx-auto max-w-5xl px-6 py-20">
        <div className="grid gap-12 md:grid-cols-2">
          <FeatureCard
            href="/docs/tutorials/configure-your-first-squeezebox"
            label="Tutorial"
            title="Configure your first Squeezebox"
            body="A 5-minute walkthrough from factory state to a working device."
          />
          <FeatureCard
            href="/docs/how-to"
            label="How-to guides"
            title="Task-oriented recipes"
            body="DHCP, static IP, Wi-Fi WPA2, multi-NIC discovery, recovery. Each page is one user goal."
          />
          <FeatureCard
            href="/docs/reference/commands"
            label="Reference"
            title="Every subcommand and flag"
            body="Authoritative lookup — discover, info, read, get, set, reboot, getip, interfaces."
          />
          <FeatureCard
            href="/docs/concepts/how-udap-discovery-works"
            label="Concepts"
            title="How it works underneath"
            body="The UDAP wire protocol, the multi-NIC story, why discovery uses limited broadcast."
          />
        </div>
      </section>
    </main>
  );
}

function FeatureCard({
  href,
  label,
  title,
  body,
}: {
  href: string;
  label: string;
  title: string;
  body: string;
}) {
  return (
    <Link
      href={href}
      className="group rounded-lg border border-fd-border bg-fd-card p-6 hover:border-fd-foreground/30 transition-colors"
    >
      <div className="text-xs uppercase tracking-widest text-fd-muted-foreground mb-2">
        {label}
      </div>
      <div className="text-xl font-semibold text-fd-foreground mb-3 group-hover:underline">
        {title}
      </div>
      <p className="text-fd-muted-foreground text-sm leading-relaxed">{body}</p>
    </Link>
  );
}
```

- [ ] **Step 2: Verify the landing page renders cleanly in both dark and light mode**

```bash
pnpm dev
```

Visit `http://localhost:3000`. Toggle the theme switcher (Fumadocs
provides one by default in the header) and confirm the colours work in
both modes.

- [ ] **Step 3: Commit**

```bash
git add docs/site/app/\(home\)/page.tsx
git commit -m "feat(docs-site): custom landing page with hero and feature cards"
```

### Task 4.2: Configure the versioning snapshot mechanism

**Files:**
- Modify: `.github/workflows/release.yml` (or whichever workflow file currently runs semantic-release)

The Release workflow needs a new post-tag step that:

1. After semantic-release cuts a version tag
2. Copies `docs/site/content/docs/` → `docs/site/content/docs/v<X.Y.Z>/`
3. Commits the snapshot to `main`
4. The next docs deploy picks it up

- [ ] **Step 1: Inspect the current release workflow**

```bash
cat .github/workflows/release.yml
```

Identify where semantic-release runs and which step outputs the new
version number.

- [ ] **Step 2: Add the snapshot step**

After the `cycjimmy/semantic-release-action@vX` step, append:

```yaml
      - name: Snapshot docs for the released version
        if: steps.semantic.outputs.new_release_published == 'true'
        env:
          NEW_VERSION: ${{ steps.semantic.outputs.new_release_version }}
        run: |
          set -e
          if [ -z "$NEW_VERSION" ]; then
            echo "No new version published, skipping docs snapshot"
            exit 0
          fi
          SNAPSHOT_DIR="docs/site/content/docs/v${NEW_VERSION}"
          if [ -d "$SNAPSHOT_DIR" ]; then
            echo "Snapshot $SNAPSHOT_DIR already exists, skipping"
            exit 0
          fi
          cp -r docs/site/content/docs "$SNAPSHOT_DIR.tmp"
          # Remove any nested vX folders from the copied tree to avoid
          # snapshot-inside-snapshot infinite-Russian-doll
          find "$SNAPSHOT_DIR.tmp" -maxdepth 1 -type d -name 'v[0-9]*' -exec rm -rf {} +
          mv "$SNAPSHOT_DIR.tmp" "$SNAPSHOT_DIR"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "$SNAPSHOT_DIR"
          git commit -m "docs(site): snapshot v${NEW_VERSION}"
          git push
```

Verify the YAML indents correctly with the existing file.

- [ ] **Step 3: Test the snapshot logic locally**

```bash
# Dry-run the snapshot copy
SNAPSHOT_DIR="docs/site/content/docs/v0.0.0-test"
cp -r docs/site/content/docs "${SNAPSHOT_DIR}.tmp"
find "${SNAPSHOT_DIR}.tmp" -maxdepth 1 -type d -name 'v[0-9]*' -exec rm -rf {} +
mv "${SNAPSHOT_DIR}.tmp" "$SNAPSHOT_DIR"
ls "$SNAPSHOT_DIR" | head
# Cleanup
rm -rf "$SNAPSHOT_DIR"
```

Expected: the test snapshot has all the normal subdirectories but no
nested `vX.Y.Z` folders.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci(release): snapshot docs/site/content/docs per released version"
```

### Task 4.3: Hide versioned snapshot trees from the Latest sidebar

**Files:**
- Modify: `docs/site/lib/source.ts` (the Fumadocs source object — the file Fumadocs's CLI created during Task 1.1)

By default, Fumadocs builds the sidebar nav by walking every directory
under `content/docs/`. Once Task 4.2's snapshot mechanism runs in CI,
`content/docs/v<X.Y.Z>/` directories will accumulate and appear in the
Latest sidebar — confusing.

The fix is a path filter on the source's nav generation. Versioned URLs
still work (Fumadocs auto-routes every MDX file by directory path) — they
just don't appear in the Latest sidebar.

- [ ] **Step 1: Inspect the current `source.ts`**

```bash
cat docs/site/lib/source.ts
```

The scaffolded file from Task 1.1 looks like (exact wording may differ slightly):

```typescript
import { docs } from '@/.source';
import { loader } from 'fumadocs-core/source';

export const source = loader({
  baseUrl: '/docs',
  source: docs.toFumadocsSource(),
});
```

- [ ] **Step 2: Add a path filter that excludes versioned directories from the Latest tree**

Modify to:

```typescript
import { docs } from '@/.source';
import { loader } from 'fumadocs-core/source';

// Latest tree: every directory under content/docs/ EXCEPT versioned
// snapshots (vX.Y.Z/). Versioned URLs still work (Fumadocs's file-based
// routing emits them automatically); they just don't show up in the
// Latest sidebar.
const latestFiles = docs.toFumadocsSource().files.filter(
  (file) => !/^v\d+\.\d+\.\d+(-rc\.\d+)?\//.test(file.path),
);

export const source = loader({
  baseUrl: '/docs',
  source: {
    files: latestFiles,
  },
});
```

The regex `^v\d+\.\d+\.\d+(-rc\.\d+)?\//` matches paths starting with
`v1.2.3/` or `v1.2.3-rc.1/` — the snapshot directory shape.

- [ ] **Step 3: Verify the Latest sidebar is unaffected**

```bash
pnpm dev
```

Visit `/docs` — the sidebar shows Tutorial / How-to / Reference / Concepts / Contributing as before. No `v1.8.0` entry appears at the top level.

- [ ] **Step 4: Verify versioned URLs still resolve**

After Task 4.4 creates the initial `v<current>/` snapshot, visit
`/docs/v<current>/`. The page renders (just without the Latest sidebar
context — its own nav comes from the snapshot directory's own structure).

This is acceptable for M4 — archived versions are static-ish snapshots
not meant to be daily-browsed. Future iteration can build per-version
sidebars if there's demand.

- [ ] **Step 5: Commit**

```bash
git add docs/site/lib/source.ts
git commit -m "feat(docs-site): hide versioned snapshots from Latest sidebar"
```

- [ ] **Step 3: Add a version selector to the docs layout**

Edit `docs/site/app/docs/layout.tsx` (or whatever Fumadocs uses for
docs-wide layout). Add a small dropdown in the header. For v1, the
list can be hardcoded — entries added by the release workflow as new
versions are snapshotted.

```typescript
// In the docs layout
import { VersionSelector } from '@/components/version-selector';

// ... existing layout JSX
<HeaderSlot>
  <VersionSelector />
</HeaderSlot>
```

Create `docs/site/components/version-selector.tsx`:

```typescript
'use client';
import { useState } from 'react';
import Link from 'next/link';

const VERSIONS = [
  { label: 'Latest', href: '/docs' },
  // Versions get appended here by the release workflow's snapshot step.
  // For now, hardcode the most recent release as a placeholder:
  { label: 'v1.8.0', href: '/docs/v1.8.0' },
];

export function VersionSelector() {
  const [open, setOpen] = useState(false);
  return (
    <div className="relative">
      <button
        onClick={() => setOpen(!open)}
        className="px-3 py-1 text-sm border border-fd-border rounded hover:bg-fd-card"
      >
        {VERSIONS[0].label} ▾
      </button>
      {open && (
        <div className="absolute right-0 mt-1 w-40 bg-fd-card border border-fd-border rounded shadow-lg">
          {VERSIONS.map((v) => (
            <Link
              key={v.href}
              href={v.href}
              className="block px-3 py-2 text-sm hover:bg-fd-background"
              onClick={() => setOpen(false)}
            >
              {v.label}
            </Link>
          ))}
        </div>
      )}
    </div>
  );
}
```

NOTE: this version selector is intentionally minimal. A future
iteration would auto-derive the version list from the
`content/docs/v*` directories or from git tags. For v1, hardcoding
is fine because the snapshot workflow will update this file
alongside the snapshot directory.

- [ ] **Step 4: Update the snapshot workflow to also update the version list**

This is an enhancement to Task 4.2's snapshot step. After the
snapshot directory is created, also append the new version to
`docs/site/components/version-selector.tsx`'s `VERSIONS` array. The
implementer should add a `sed` or `node` script step in the
workflow.

- [ ] **Step 5: Verify the version selector renders**

```bash
pnpm dev
```

Click the version selector; confirm dropdown opens; clicking "Latest" stays on `/docs`; clicking the hardcoded version goes to `/docs/v1.8.0` (which won't exist yet — that's fine for now).

- [ ] **Step 6: Commit**

```bash
git add docs/site/components/version-selector.tsx docs/site/app/docs/layout.tsx
git commit -m "feat(docs-site): add version selector dropdown"
```

### Task 4.4: Take the initial snapshot for the current version

**Files:**
- Create: `docs/site/content/docs/v<current>/...` (directory tree, copied from `docs/site/content/docs/`)

For the M4 PR to land with a working version selector pointing at a
real snapshot, take the first snapshot manually before merge.

- [ ] **Step 1: Determine the current version**

```bash
git describe --tags --abbrev=0
```

Note the result (e.g. `v1.8.0`).

- [ ] **Step 2: Run the snapshot manually**

```bash
VERSION=$(git describe --tags --abbrev=0 | sed 's/^v//')   # e.g. 1.8.0
SNAPSHOT_DIR="docs/site/content/docs/v${VERSION}"
cp -r docs/site/content/docs "${SNAPSHOT_DIR}.tmp"
find "${SNAPSHOT_DIR}.tmp" -maxdepth 1 -type d -name 'v[0-9]*' -exec rm -rf {} +
mv "${SNAPSHOT_DIR}.tmp" "$SNAPSHOT_DIR"
```

Update the version selector's `VERSIONS` array to include this version's URL.

- [ ] **Step 3: Verify the snapshot is browsable**

```bash
pnpm dev
```

Visit `http://localhost:3000/docs/v1.8.0`. Expected: the docs landing page renders at that URL.

- [ ] **Step 4: Commit**

```bash
git add docs/site/content/docs/v* docs/site/components/version-selector.tsx
git commit -m "docs(site): take initial v<X.Y.Z> snapshot"
```

### Task 4.5: Push and open the M4 PR

- [ ] **Step 1: Final build**

```bash
cd docs/site
pnpm install --frozen-lockfile
pnpm build
```

Expected clean.

- [ ] **Step 2: Push and open PR**

```bash
git push -u origin feat/docs-site-m4-versioning
gh pr create --title "feat(docs): custom landing page and versioning snapshot mechanism" --body "Closes M4 of Task #28. Final milestone.

## Custom landing page
- Hero with strong typography (Fumadocs theme colours)
- Feature cards for Tutorial / How-to / Reference / Concepts
- Works in light and dark mode

## Versioning snapshot mechanism
- Release workflow now snapshots docs/site/content/docs/ → docs/site/content/docs/v<X.Y.Z>/ on every published release
- Initial snapshot of current version (v<X.Y.Z>) taken manually as part of this PR
- Version selector dropdown in the docs header; entries auto-appended by future releases

## Test plan
- [x] Landing page renders cleanly in light and dark mode
- [x] Initial snapshot accessible at /docs/v<X.Y.Z>/
- [x] Version selector dropdown works
- [ ] After merge: next release tag triggers a fresh snapshot via the workflow"
```

---

## Final closeout

After all four milestones merge:

- [ ] Update Task #28 (tracker) → completed
- [ ] Consider opening Task #34 (ad-hoc Wi-Fi onboarding) and Task #35 (release-please switch) when ready
- [ ] Optionally tweet / post the docs site URL to project users
- [ ] Watch site analytics (if any added later) for what users actually read
