# DHCP on wired Ethernet — minimal example
# Use with: go-udap set <mac> --config dhcp.conf --reboot
#
# A factory-fresh device defaults to wired (interface=1) and DHCP
# (lan_ip_mode=1), so this file only sets the music server address.

server_address=192.168.1.250
